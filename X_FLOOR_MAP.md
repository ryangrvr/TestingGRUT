# X_FLOOR_MAP — the decision instrument for "what pins x?"

*Status: **INSTRUMENT, ledger 0** (per the front's own trap fence: a decision procedure banks at ledger 0 or not at all — it is furniture until a computation fires a predicate). No register edit accompanies this document. Provenance: the 2026-08-03 x-floor reconnaissance (four route-mappers + adjudicator, every load-bearing register claim re-verified; full record in the session workflow journal). The overseer rules on the attack order before any calc is built.*

<!-- REGISTER-SYNC: 53 nodes, net +16 -->
<!-- REGISTER-TOTAL: 74 = 53 grut + 21 vacuum-cluster; nets +16 grut, +0 cluster -->

---

## The question and its asymmetry

The interior family μ(x) = 1 + xα **allows** up to the computed window and **predicts nothing** (x has no floor). GRUT-as-written pins x = 0 **by constitutive choice** (p_tt CHOSEN, 2026-08-02). The x-floor question is therefore not "is x zero?" but: **does any derived structure FORCE x — to zero (vindicating the choice, graduating the ΛCDM claim) or to a value (contradicting GRUT-as-written and creating its first prediction)?** The overseer's decision criterion, pre-registered: pinned → the program continues on new footing; nothing pins → deposit as ontology + four derived boundaries + a computed allowance window + a method.

## The four routes

| route | decisive object | tractability | honest odds (default-broken) |
|---|---|---|---|
| **R1 — action map** | x_anom := c₀_anom / c₀(x=1): the DC P⁰ˢ-projected retarded kernel induced by the anomaly-sourced conformalon (Riegert/Antoniadis–Mottola normalization Q² = −2b′, vertex from g = e^{2σ}ĝ) against the inherited bookkeeping coupling | **in-house-hard** (fenced to the flat/fixed-slicing DC bookkeeping level; hard halt-and-dispatch on the Bardeen completion or any contested-dS need) | ~70–80% no-pin: scheme-dependent DC (rung9b's tertiary obstruction, now *computed* in the spin-0 sector) or a k-structured form factor (the constant-x family exposed as bookkeeping-only). ~10–20%: a scheme-immune x_anom — the one conditional-prediction path |
| **rung3 — bath microphysics** | Π₀(ω→0, k→0): the P⁽⁰ˢ⁾ coefficient of the gauge-invariantly assembled retarded influence kernel of the pure-dS graviton sector — ⟨[T^μ_μ, T^ν_ν]⟩_R of exactly what GRUT integrates out | **frontier-blocked** (strictly harder than the TT spine: gauge-dependent sector, contested secular growth, no assembled scalar observable in the literature); an in-house tranche exists (see attack order 3) | modal: earned-under-determined, same dS-IR controversy, worse-controlled channel. "Π₀ = 0 forced" nearly dead on banked grounds (no protecting symmetry survives the no-protection enumeration); Π₀ ≠ 0 *established* is also far (assembly frontier) |
| **u5 — dynamics (R3)** | the x-homogeneity theorem: causality/KK, passivity, KMS, Onsager act channel-diagonally on orthogonal projectors as convex-cone (amplitude-homogeneous) conditions | **in-house-now** (`calc/x_no_pin.py`, extends operator_basis.py) | **classifier, not pinner** — high confidence. Real yields: the theorem itself (a D3 completion-bar item). ~~the x ≥ 0 orientation lemma~~ — **RULED UNBANKED 2026-08-04** (see the Riders note below) |
| **fork logic** | the decision functional D(Π₀, x*, x_edge^auto) → {FORCED, PREDICTED, KILLED-ON-ARRIVAL, DEPOSIT} with machine-checkable entry predicates | **in-house-now** (this document + the frozen TT-auto gate) | modal terminal: D3 (deposit) or D1′ (demotion); the instrument's realistic yield is an *honest deposit criterion*, not a pin |

## The decision tree (all tier/ledger consequences NAMED, never executed)

**D1 — x = 0 FORCED.** Predicate: an exhibited **non-perturbative** vanishing mechanism for ⟨T^μ_μ T^ν_ν⟩_R that survives the no-protection enumeration (a *new identity* — the rung9b-rescue bar; perturbative/leading-order/gauge-artifact results do not fire this). Consequence: c₀=0 derived; p_tt's +1 retires at home, a new +1 books at rung3 (relocation priced, net ~unchanged); `mu_linear` named-for-graduation *conditional on the rung3 commitment*; the interior closes as legitimate foreclosure. Pre-registered cross-check: a return landing "stability horn AND Π₀=0 forced" owes a decoupling explanation (one controversy cannot fund both single-pole and forced-c₀=0).

**D1′ — DEMOTION (first-class terminal).** Predicate: Π₀ ≠ 0 **ESTABLISHED** (gauge-invariantly assembled, class-only) — the literal armed-trigger predicate on `mu_linear`. The trigger fires mechanically (`derived-pending → assumed`); firing on under-determined or conditional-on-commitment returns is the TRIGGER-LAUNDERING kill.

**D2 — x\* COMPUTED** (R1 scheme-immune at conditional-theorem grade inheriting rung9a's IF; or rung3's value branch via the banked x = f(Π₀/Π₂) map — the α-backdoor is forbidden). Sub-fork on the **frozen** TT-auto edge:
- **D2a, x\* > x_edge^auto: KILLED ON ARRIVAL** by existing Planck low-ℓ TT — the program's first completed pre-registered kill-test. Failure settles against the conformal-mode-as-carrier bundle (feeds rung9a's conditional), not against the family per se. Banked warning: rung3's value branch likeliest lands here (spin-0 secular structure is O(G) vs TT's O(G²)).
- **D2b, x\* ≤ x_edge^auto: GRUT's first positive prediction** — μ−1 = x\*/3, Σ−1 = x\*/6, banked at conditional-theorem grade and never "forced"; the slip number quotable only after the R2 recompute. Discriminator: the TT-auto channel itself on existing Planck data (immediate); DESI DR3 corroborates ~0.5–1σ at x\*~0.1; CMB-S4 low-ℓ adds ~nothing. Special case x\* = α²: maximum directional-guard pressure — full KC battery + independent reproduction before banking.

**D3 — NOTHING PINS (modal).** Predicate: R1 returns scheme-dependent or k-structured; rung3 frozen at earned-under-determined or unresourced; the x_no_pin theorem banked. Consequence: **DEPOSIT, gated on the completion bar**: (i) TT-auto landed computed-grade; (ii) x_no_pin theorem banked (R3 closed as classifier); (iii) the no-protection enumeration banked ("FORCED requires an unnamed new identity" — earned); (iv) the separate-universe residue landed or marked owed-permanent; (v) rung3 frozen with the sibling brief attached as named blocker; (vi) the ungated u5 general classification run; (vii) the `mu_linear` dissent preserved verbatim + the CONDITIONAL-on-CHOSEN qualifier on every ΛCDM statement. Deposit shape: ontology + four derived boundaries + a computed allowance window with no floor + a method demonstrated on itself — *allowing, never predicting*.

**Riders:** ~~the x ≥ 0 orientation lemma banks as a tier-0 addition FENCED on R1's sign orientation~~ — **RETRACTED AS A STANDING INSTRUCTION, 2026-08-04 (overseer-ruled; recorded in `zeta_interior_family.boundary_condition`).** R1 has run and **declines to supply a sign** (it flips with the free counterterm), and the stronger finding is that the fence was **mis-homed**: R1's sign is the sign of a quantity ~10⁻¹¹⁴, which could not orient the family's physical dial even if definite. **The lemma is NOT to be banked by a future wave on this instruction.** It may return only if its fence finds a real home (candidate: the passivity argument standing alone), screened on its own merits, never inherited. R1's partial-pin outcome (x_anom = 0 scheme-immune) pins only that *the anomaly does not source the interior* — the fiat becomes better-motivated, stays a fiat; no tier change.

## The attack order (adjudicated)

> **A8 DEMOTION (2026-08-09, the κ wave — `provenance/prereg/RESULT_KAPPA_2026-08-08.txt`):** the item below's headline **"UNCONDITIONAL x < 0.358" is DEMOTED on three defects**: the κ range is unbounded in banked physics (edges 0.55/0.90 at κ=10/30 — scan-relative, κ≲6); the sharp θ-step is excluded as a shape (Paley–Wiener-grade, no replacement licensed); and the filter is an **unbanked world-model insertion** inside a frozen gate (P2 is exact line-of-sight — there is no approximation being guarded), sitting exactly in the ISW signal band (k/aH ∈ [1.7, 15.7]). **No unconditional x-bound exists from the TT-auto gate**; every number it emits is insertion-contaminated (GRUT-plus-filter, not GRUT-as-banked) and any consumer must declare the insertion. D2's sub-fork loses its unconditional branch pending the activation-scale frontier (two blocked inputs: c₀'s k-structure — the bath's; the covariant completion — frontier-reserved). κ ≥ 1 survives as a floor (never a value). The paragraph below is retained unedited as the frozen record.

1. ~~**The rigorous TT-auto calc**~~ — **LANDED 2026-08-03** (`calc/isw_tt_auto.py`, `RESULTS_isw_tt_auto.md`): first-freeze constants VOIDED by their own firewall (Bessel-recurrence bug → numerical noise), rebuilt under amendment record A1–A7, **re-frozen** (verified still pre-candidate, so no retro-fit). **What the gate delivers:** an UNCONDITIONAL bound **x < 0.358** (kills any computed x\* above it at every declared member) and κ-CONDITIONAL verdicts below it (κ=1 edge 0.037 ⇒ x=α² dead at 6.2σ; κ=3 sharp edge 0.358 ⇒ x=α² alive at 0.86σ). Normalization is not a lever (<1% over the KC-band); **the filter is the entire systematic**. Two consequences for this instrument: **(a) D2's sub-fork is now three-valued** — x\* > 0.358 = KILLED unconditionally; x\* < 0.037 = SURVIVES all members; 0.037 ≤ x\* ≤ 0.358 = **VERDICT PENDING the activation-scale question** (below). **(b) two owed items attach to the gate**: the A6 common-mode (Boltzmann-grade differential) check before any κ=3 kill-grade use, and —

   **1b. THE ACTIVATION-SCALE QUESTION (new named owed item, in-house, HIGH VALUE).** The κ-lever is the difference between GRUT's natural point being dead and alive. κ parameterizes *where the quasi-static P⁰ˢ response actually activates* — currently a generic-validity guess (k > κaH). But GRUT's response kernel carries its own banked memory structure (`rung3_single_pole`, the finite-memory L₀), so the crossover may be **GRUT-derivable rather than free**. If the kernel fixes κ, every κ-conditional verdict becomes unconditional and the gate regains its teeth. Proposed as attack item 1b, ahead of items 2–3.

   *(Original rationale, retained:)* **+ this instrument, same wave.** The gate every terminal consumes: the D2 kill threshold, the D3 window grade, and the adjudicator of x=α² (which sits in/above the estimate band 0.03–0.14). **Amended pre-registration** (the strict before-any-candidate freeze partially lapsed — the PART-F estimate exists and x=α² is already named): pipeline choices sourced to external literature standards with rationale in the file header; filter/normalization sensitivity scanned as a KC-band; the quotable edge taken at the conservative band member; **frozen before any R1/rung3 x\* computation runs** (the anti-retro-fit ordering — the band is filter-sensitive by ~4×, a lever the kill-test must not have).
2. ~~**`calc/x_no_pin.py`**~~ — **LANDED 2026-08-09** (`calc/x_no_pin.py`, `calc/RESULTS_x_no_pin.md`; pre-registered and sealed BEFORE the build, `PREREG_X_NO_PIN_2026-08-09.txt`; scored outcome (a) FLOOR ONLY — the default-broken branch). **The channel-diagonal passivity lemma**: matrix passivity ⟺ ω·Im c₂ ≥ 0 AND ω·Im c₀ ≥ 0 independently, NO cross-channel rescue (min eigenvalue pinned at the violating channel through 10⁶ amplification); the admissible set is a convex cone — **no ceiling, no pin: route R3 CLOSED as classifier, its no-pin fragment now COMPUTED, not inherited**. Register homes, POST-RULING 2026-08-09: **SPLIT by overseer ruling** — the first single-node draft conflated the general lemma with its application and the bank gate's TIER-CONTRADICTION was upheld, not overridden (the builder's cited-not-edged precedent was mis-read: `zeta_interior_family` cites-not-edges *mu_linear* against BUILDS-ON-CLOSED; it does edge the enumeration). Now: `passivity_channel_diagonal` (the general lemma, shown, frame-free, Δ0) + `x_no_pin_theorem` (the application, derived-pending on the enumeration's 4d-covariant frontier — the gate's own suggested tier, Δ0) + `kk_static_transfer` (Δ0: the dissipative-to-STATIC transfer question — ANSWERED 2026-08-09, `calc/kk_static_transfer.py`, staged derived-pending at the gate: unconditional transfer REFUTED by passive counterexample (negative contact term, invisible to passivity/KMS); conditional transfer DERIVED at the tightest class-level criterion **χ_∞ ≥ 0** (sufficient, not necessary — passivity supplies no necessary condition on χ_∞; rung3's single-pole family is a sufficient subclass, not the boundary — the floor does not die if single-pole falls); the unconditional quote stays forbidden permanently). The orientation lemma's re-homing: the passivity fence is now real, and what it supports is exactly the definitional restatement x_diss(ω) ≥ 0 **under S_IF.md's declared x ≡ normalized-c₀ construction** — pointwise on a function, never a bare number; the 2026-08-04 retraction of the R1-fenced version STANDS. Own firewall event: the first PSD tolerance faked a CEILING (the pre-registered flattering direction) and the frozen-verdict selftest caught it. The Israel–Stewart ceiling is reclassified OUT of in-house reach (three named background inputs, none in the corpus) → `SPECIALIST_BRIEF_rung3_spine.md` Rider C. *(Original scoping, retained:)* the cheapest determinate result on the board; converts the register's fragments into the earned no-pin theorem; carries the fenced orientation lemma.
3. **`calc/pi0_trace_channel.py` in-house tranche + draft `SPECIALIST_BRIEF_rung3_trace_channel.md`**: (a) the x = f(Π₀/Π₂) normalization map — *convention-level bookkeeping, ledger 0, explicitly NOT R1 closure*; (b) Isaacson-level power counting + the two-graviton J=0 cut as the first candidate nonzero trace-channel weight; (c) the no-protection enumeration. Either exhibits a survivor or earns "FORCED requires an unnamed new identity."
4. ~~**`calc/anomaly_c0_map.py`** (R1)~~ — **LANDED 2026-08-03** (`RESULTS_anomaly_c0_map.md`). **R1 RETURNS NO PIN**: `result_face = k-dependent form factor`, x_anom(k) = [1/(3α)]·k²/(k²+M_σ²), DC zero scheme-immune *in flat space* (curved HALTED). The anomaly's own contribution is **≥108 decades** below every banked bound; the scheme piece is free and unbounded — *that freedom is the finding*. **α-power = 0 as an output** (b′→P0s only, b→P2 only, so a/c is the coefficient of nothing); **x = α² structurally unreachable** — the confirmation-bias trap did not spring. Scope split enforced: **(A) local ≤4-derivative flat space = UNCONDITIONAL** and carrier-agnostic; **(B) nonlocal closure = CONDITIONAL on rung9a's IF**, gap closed by dimensions; **(C) curved = HALTED**. Fires the **partial-pin rider** (partial predicate match, stated) and **both D3 predicates**; no tier change; ledger 0. *Its own firewall went AMBER on eight blockers — the first build's load-bearing facts were print statements and a mutation test replaced the answer with x=α² undetected; rebuilt, now mutation-tested against five mutants.* **New blocker it raised: the x ≥ 0 orientation lemma's fence was mis-homed on R1's sign** (which is scheme-degenerate, and ~10⁻¹¹⁴ regardless) — re-home or unbank, overseer's call. **Two proposed register edits held:** a refinement (strengthening) of `rung9b_bridge`'s PRIMARY obstruction via the computed a/c sector-split, and a correction to `eft_operator_basis` finding (iv)'s two-point clause.

   *(Original scoping, retained:)* (R1, six steps: σ–h vertex; Q²=−2b′ reused from `conformalon_q2_band.py`; projection via `operator_basis.py` with the c₂-receives-zero guard; DC limit in the inherited bookkeeping; **scheme scan with the local-Weyl-term coefficient free**; k-structure check). Run fourth *deliberately*: any x_anom lands against a pre-frozen edge. Halt-and-dispatch: Bardeen completion → extend SPECIALIST_BRIEF_2; contested-dS need → never in-house.
5. **Dispatch decisions, strictly after 1–4**: the trace-channel sibling brief goes out only if resourced (frontier-blocked, harder than the TT spine); the u5/u6 KNOB1 fixed-point calc stays last (z=1 unproven; success pins x=0 only asymptotically). **D3 requires no dispatch to complete** — the front is not hostage to specialist availability.

## Pre-registered kill-conditions (per route, condensed; carried verbatim into each calc header at build time)

**R1:** NATURALNESS-BY-FIAT (the α-power must be an *output* of the contraction — x=α² carries live confirmation-bias pressure); SCHEME-LAUNDERING (choosing the scheme that yields a definite x relocates the fiat); RUNG9B BACK-DOOR (nothing transports α into c₂ — the map lives in the spin-0 sector or dies); CONDITIONAL-LAUNDERING (rung9a's IF is an input, not established); FIT-NOT-PIN (a k-structured kernel projected onto best-fit constant x is a fake prediction); FRONTIER TRESPASS (the contested dS sector escalates to dispatch, never approximated in-house).

**rung3:** RELOCATION (a propagating-conformalon Π₀ banks only conditional-on-commitment, +1 priced); SECULAR-SHORTCUT (O(G) ln(a) growth in the gauge-dependent sector is not an established Π₀≠0); DOF-COUNTING (KC2-refuted; must survive the two-graviton J=0 cut); KINEMATIC-AVERAGING (Isaacson traceless-at-leading-order is not vanishing-to-all-orders); WEYL (anomaly-blocked, banked); HORN-CONFLATION (gauge-artifact removal ≠ vanishing theorem); ALPHA-BACKDOOR (the value branch normalizes by Π₀/Π₂, never by α); CONSTRAINT-CONTAMINATION (contact/constrained response is not spectral content); TRIGGER-LAUNDERING (the trigger fires on ESTABLISHED only).

**u5:** ORGANIZATION-AS-PIN (shape classification constrains χ₀'s ω-shape, not the DC amplitude x); SIGN-LAUNDERING (R1 owns the sign orientation); DOUBLE-HOMING (the rung3 escape is banked at p_tt_ansatz, count-once); TOY-TO-THEOREM (u5u6_deformability banks z=1 as assumption); PLACEMENT-BEFORE-GATE (vacuum placement is rung3-gated); ATTRACTOR-AS-PIN (asymptotic attraction ≠ measured-scale pin).

**Fork:** ENTRY-PREDICATE VAGUENESS (every branch names the register field whose change fires it); DEFAULT-BRANCH LAUNDERING (FORCED is not the expected terminal); AUTO-GATE RETRO-FIT (the pipeline freezes first); PREMATURE DEPOSIT (the bar is a completion bar, not an exit ramp); INSTRUMENT-AS-RESULT (this document is ledger 0).

## Trap fences (the front-wide laundering shapes, pre-registered)

WINDOW-SHRINK NATURALNESS (a shrinking upper bound with no floor selects nothing); MINIMALITY/OCCAM FOR TT-ONLY (refuted in advance: linearized EH itself = (1/2)k²[P⁽²⁾ − 2P⁽⁰ˢ⁾] — GR-relative minimality favors a *nonzero* scalar modulus); SCREEN-LOOKS BANKING ("looks stable" never graduates x=0); TRIGGER-AVOIDANCE-AS-SUPPORT (rung3 staying open is not support for the choice it would rescue); BEST-FIT-CONSTANT-X; NATURAL-SCHEME SELECTION; **ANOMALY-ACTIVE PRIOR AS LEANING** (adjudicated: GRUT's α import argues *incoherence of motivation* for c₀=0 only — citing it as an established leaning toward Π₀≠0 is the α_M/ImΣ_R-class category slip); CONDITIONAL-INTO-ANCHOR (every pinned/predicted x carries rung9a's IF + the CONDITIONAL-on-CHOSEN qualifier downstream); ORIENTATION-AS-FLOOR.

## Adjudicated disagreements (recorded)

1. **The anomaly-active prior**: two mappers leaned on "GRUT imports α as the trace anomaly, so Π₀=0 is the surprising outcome." Adjudicated as usable only as *incoherence-of-motivation on c₀=0*, never as an established leaning on any branch (see trap fence above).
2. **The payoff map is not R1 closure**: the x = f(Π₀/Π₂) normalization map (attack item 3a) banks as convention-level bookkeeping at ledger 0; the action-level x↔c₀ map (attack item 4) is a different object on a different lineage. Count once, home separately.
