# GRUT II — what survived: the Version II close

*Companion to [`GRUT_I_What_Survived.md`](GRUT_I_What_Survived.md), same discipline: a **presentation layer over `provenance/claims.json`** — no new content, no new physics, one label per claim, checked against the machine register. V1 deposited the ontology and its first boundaries. **V2 deposits what the in-house program could still answer about itself.** The x-floor is closed with computed results on the routes the program could reach in-house; what remains is one external computation (dispatch-ready) plus one secondary open question — see the headline.*

<!-- REGISTER-SYNC: 53 nodes, net +16 -->
<!-- REGISTER-TOTAL: 74 = 53 grut + 21 vacuum-cluster; nets +16 grut, +0 cluster -->

> **The honest headline.** GRUT is a **unifying ontology with derived boundaries whose entrance into observation now rests on one external computation and one secondary open question.** Zero novel positive predictions. Signature-null on positive signatures; **the falsifiable content is the no-go directions.** What V2 adds over V1 is not a prediction — it is that the program now knows *which* computation stands between it and a prediction, and knows it by having closed the routes it could reach.
>
> **Two items remain live, not one** (stated against the temptation to round down): (1) **rung3's Π₀** — external, frontier-blocked, dispatch-ready; (2) **the κ activation-scale question** — which may be partly in-house, and which would make the observational gate's verdicts unconditional rather than κ-conditional. The register's own planning map (`X_FLOOR_MAP.md`) and `STATE.md` both carry both.

---

## 1. Four derived boundaries (⊘) — the GRUT-specific output

Each is a computed or obstruction-backed **negative**. They are the framework's genuine content: walls of the theory, not decorations.

| boundary | node | grade |
|---|---|---|
| **The GW-dissipation invisibility limit** | `rung4_love_kk` (tier `shown`); `calc/gw_dissipation_bounds.py` | computed |
| **The α→TT bridge is settled-negative** | `rung9b_bridge` | obstruction-backed (**not** a formal no-go) — mechanism *refined* 2026-08-03 by the computed a/c sector-split, **grade explicitly unmoved** |
| **An economical (sub-CPL) evolving w(z) is not earned** | `rung7_wz` | structural; holds regardless of pole structure |
| **μ = 4/3 is excluded** | `mu_linear` | multi-leg, re-graded 2026-08-03: ISW-cross **computed ~2.0σ** + DESI Σ₀ ~3.5σ (joint ~4σ-class) + separate-universe no-go (EdS-quantified, conditional). **p_tt-INDEPENDENT** |

Held **open**, deliberately not boundaries: decoherence invisibility (rung3-conditional), the no-crossing and wₐ-sign (rung3-gated), μ=1=ΛCDM (p_tt-gated, and now known **conditional on a CHOICE**).

## 2. The arrow's existence/direction decomposition

Dissipation's **existence** is intrinsic to the responsive-vacuum ontology (shown-grade); its **direction** is imported (the Past Hypothesis, a Bin-1 primitive). GRUT *locates* the assumption precisely rather than deriving the arrow. This is a genuine contribution to a live problem: it says exactly which half of the arrow question an open-system vacuum can answer and which half it cannot, and it does not pretend the second half was earned.

## 3. `p_tt` is CHOSEN — the load-bearing structural result

The program's most important single finding, and it is a **demotion**:

- **Diffeomorphism invariance buys transversality, not tracelessness.** Imposing the diffeo Ward identity on both Schwinger–Keldysh branches leaves exactly **two** survivors of the 6-dimensional Barnes–Rivers space: P⁽²⁾ and the traceful P⁽⁰ˢ⁾.
- **Linearized Einstein–Hilbert itself carries a scalar kernel twice its spin-2 one** — −h·G⁽¹⁾ = k²[−½P⁽²⁾ + 1·P⁽⁰ˢ⁾], the GR lock P0s/P2 = −2, verified in exact rationals by four independent implementations. GR's own kernel is not TT-only, so no general principle can force TT-only.
- **Therefore ζ_vacuum ≡ 0 is a constitutive assertion, not a symmetry consequence.** GRUT foreclosed the scalar sector by fiat.

The symmetry route to FORCED is definitively closed. One dynamical route survives (a bath trace-correlator vanishing), and it would **cost a new +1 at rung3** — relocating the assumption rather than discharging it.

## 4. The 4d operator-basis enumeration

The complete Lorentz-covariant kernel space is the 6-dimensional Barnes–Rivers space; Ward leaves exactly {P⁽²⁾, P⁽⁰ˢ⁾}, so the admissible family is **K = c₂P^TT + c₀P⁰ˢ** and *restricting c₀ = 0 is an input, not a consequence*. Carrying the 2026-08-03/04 correction (see *retractions* below): the a-anomaly (b′) reaches spin-0 only and the c-anomaly (b) reaches spin-2 only, so **α = a/c is a ratio of coefficients living in different channels — the coefficient of neither.**

## 5. The interior family — a computed allowance window, honestly labeled

The parameter space c₀ = 0 foreclosed is real, parameterized, and data-bounded: μ(x) = 1 + xα is exact in the inherited bookkeeping; **η = 1/(1+xα) and Σ = 1 + xα/2 (and with them the identity Σ−1 = (μ−1)/2) are INHERITED-CONDITIONAL on that bookkeeping's one-sidedness** — a genuine P⁰ˢ admixture could split the law via scalar anisotropic stress (R2, open). Only the μ law is unconditional.

**The label, stated without softening: the family ALLOWS up to the edge; it PREDICTS nothing.** x has no floor, so no detection confirms GRUT and no null refutes it. This is the audit's fourth category — *constrained to a computed window* — parameter space bounded by measurement rather than deleted by fiat.

**The BANKED window edge is x < ~0.59** (DESI Σ₀-lensing-bound, central-inputs and loose-upper per its named mapping fence) — that is the number in `claims.json`, and it is what this deposit stands on.

> **A8 (2026-08-09):** the gate's "unconditional x < 0.358" is **DEMOTED** — κ-range unbounded above, θ-shape excluded, and the filter ruled an **unbanked insertion** (no approximation exists in the pipeline to guard). No unconditional x-bound from this gate; all its numbers are insertion-contaminated and family-conditional. See `provenance/prereg/RESULT_KAPPA_2026-08-08.txt` and amendment A8 in the calc. The paragraph below is the pre-A8 record, retained.

*Separately, and NOT banked:* a rigorous low-ℓ TT-auto gate (`calc/isw_tt_auto.py`) is **HELD at FLAG — nothing from it has propagated to the register.** Held numbers, quoted as held: an unconditional bound x < 0.358 at the conservative filter corner, with every named-point verdict below it **κ-conditional by a factor ~10** (κ=1 → x=α² excluded at 6.2σ; κ=3 → 0.86σ). Its own record marks the full N(x) curve — not the edge — as the quotable object (N(x) is non-monotone at κ=3, the band is [0.149, 0.358] sharp-vs-smoothed), and marks a Boltzmann-grade differential check **owed** before any κ=3 kill-grade use. Normalization is not a lever (<1% across its KC-band); **the filter is the entire systematic.**

## 6. R1's no-pin — with the burden localized

The anomaly action does **not** assign the interior modulus a value. It realizes the dial as a **scale, not a number**: x_anom(k) = [1/(3α)]·k²/(k²+M_σ²), DC-zero and scheme-immune in flat space. Both anomaly vertices carry □R ~ k⁴, exactly cancelling the 1/k⁴ Paneitz propagator and leaving a **local R²** — a dimension-4 operator, which cannot shift a dimension-2 Poisson coupling by a k-independent fraction. The anomaly's own contribution is ≥108 decades below every banked bound; the scheme piece is free and unbounded, **and that freedom is the finding**.

**The burden is localized — at the grade the source assigns it, not above.** A scheme-immune normalization needs structure the local flat-space sector does not contain; the anomaly's own non-analytic structure (C² ln□ C²) lives in the spin-2 channel. The source books this as **ARGUED-GRADE, explicitly not proven** — and it rests on a three-way scope split that must travel with it: **(A)** the local ≤4-derivative flat-space enumeration is unconditional and carrier-agnostic; **(B)** the nonlocal closure is *conditional* on the conformal-mode-as-carrier hypothesis and is closed only by a dimensional argument; **(C)** the **curved-background case is HALTED** under a frontier fence, with two in-house power counts disagreeing on its k-structure — and cosmology *is* the curved case. So the honest statement is **"this points at rung3's Π₀"**, not "Π₀ or nothing".

*Channel fence:* quasi-static f(R) gives Σ = 1 identically, so the anomaly-induced R² is a **Σ-null modification — not a member of the banked interior family** at all. The magnitude comparison against a DESI Σ₀ *lensing* bound is therefore cross-channel; immaterial to the verdict (≥108 decades survives any O(1) mismatch), but stated.

## 7. The ledger's one move: +12 → +13

The count went **up**. The single-departure-shape restriction was found to be an unbooked input and was booked as rung7's third input — the first net-ledger move in the program's history, in the direction of *more* honesty about assumptions, not less. Four double-count checks recorded.

---

## What did not survive, and what was retracted

**A retraction of an overseer-relayed finding.** The 2026-08-02 interrogation's clause *"the anomaly leaves TT unaffected"* is **false at two-point order** (∫√g C² is pure spin-2) and has been retired at both of its homes; the first half ("*the* carrier is the spin-0 channel", singular) is wrong too — there are two carriers, in orthogonal channels. What survives is the one-point fact and the stronger replacement above. The register found this while computing something else.

Zero novel positive predictions — no channel examined produced one. The ζ-numerology, the beyond-standard information principle, the conformalon unification, the economical w(z), and the α→TT bridge all failed as derivations (two survive as *boundaries*). The method's "biggest contribution" framing was retracted as an over-claim. And within V2 itself: a ~32σ ISW anchor that had never been computed was **retired** when computed (it is ~2.0σ, and the banked mechanism direction was backwards); a TT-auto gate's first freeze was **voided by its own firewall**; and the answer calc's first build was **rebuilt** after a mutation test showed its selftest would pass with the answer replaced by the one value its own directional guard forbids.

That last item produced the **calc-layer floor** (`provenance/mutation_registry.py` + `test_mutation_battery.py`). The **rule** is now standing and machine-enforced: a calc producing a load-bearing number ships a mutation battery of pre-registered wrong answers, and the battery must make its own selftest fail. **The coverage is partial, and the register says so:** 5 batteries cover the calcs behind V2's result-tier numbers; **15 further register-cited calcs are explicitly listed as OWED**, with reasons, in a published list that a frozen ceiling permits only to shrink. The rule is installed; the coverage is not complete; the OWED list is the record of that.

On installation the floor found further real gaps in calcs whose numbers were already banked — each now fixed, each with a dated in-code comment: `isw_exclusion.py` (headline pins, added because the selftest did not notice the Weyl-Σ factor being dropped), `isw_tt_auto.py` (strict filter separation, added because a bare ordering check passes when the filter is disabled entirely), `mu_slip_interior.py` (a non-tautological pin, added because the anchor was a decoy alias and a wrong value at the definition line passed). **Its own firewall then found that the floor's ratchet was a tautology** — the ceiling was computed from the list it was meant to bound, so the guard could never fail: the exact failure class the floor exists to prevent, reproduced inside the floor. Fixed to a frozen literal and verified by execution.

## The state at deposit

**45 nodes · net +13 · `validate.py` PASS (register hygiene) · 109 register tests + the mutation battery.** Every claim carries a tier, a source, and a falsifier; no laundering.

**This wave is UNACCEPTED.** `bankgate.py` stands at FLAG-FOR-FIREWALL on four nodes (with RE-OPENS on `rung9b_bridge`, exactly as designed for a settled-negative node), pending overseer verification. The register — not this document — is the product; this is its readable face.

**The register's own dissents travel with the deposit**, per its discipline: `mu_linear` carries an overseer-ordered verbatim dissent that its `derived-pending` tier is "dead in spirit even though alive in letter … exactly the flattering-label inertia the directional-optimism rule exists to catch"; and `arrow_of_time` carries the caveat that even the intrinsic-positivity result is proven relative to a passive reference state.

**What would change the picture:** rung3's Π₀, computed by someone with the dS-IR machinery. If Π₀ = 0 is *forced* non-perturbatively, c₀ = 0 becomes derived and GRUT's linear-cosmology claim graduates (at the price of a new +1). If Π₀ ≠ 0 is *established*, the armed trigger fires and `mu_linear` demotes. Either way the answer is decidable, and the brief that asks for it is written.

**The one-sentence deposit:** *a disciplined open-system framing of the gravitational vacuum, screened claim-by-claim, that produced four derived boundaries, one genuine decomposition of the arrow problem, a proof that its own load-bearing projector was a choice — and no predictions; its entrance into observation now rests on a single named external computation.*
