# P1A_EDGE_ADJUDICATION_01 — RESULT

**VERDICT: EDGE_CORRECT — both nodes, both sub-questions.**

Executed 2026-09-13 under the frozen protocol
`P1A_EDGE_ADJUDICATION_01_PROTOCOL.md` (committed `bbe49f2` before execution;
owner-commissioned 2026-09-12 with pre-registered outcomes and pre-authorized
propagation). Method as frozen: three assigned-verdict advocates (EDGE_CORRECT /
EDGE_INCORRECT / AMBIGUOUS-UNRESOLVED) + independent evidence verifier + mandatory
referee; the builder executed and did not vote. Anti-optimization held: the verdict was
argued and ruled from booked law and banked content; suite color was an output, never an
input.

## 1. The rulings, per node and sub-question

| | rung1_inin_formalism | rung2_kms_gate |
|---|---|---|
| **PRESENCE** | **PRESENT (STRONG, as-banked; not FORCED)** | **PRESENT (FORCED)** |
| **REPRESENTATION** | **CORRECT** | **CORRECT** |

**Presence, rung2 (FORCED).** All three advocates converged, both hostile ones conceding
("I cannot honestly argue absence at rung2"). KMS detailed balance is definitionally
relative to a time-translation/modular flow; the banked statement's operative objects
(ω, T, the coth(ħω/2kT) lock) are flow-relative; and the node's own
`overturning_computation`, `gate/kms.py`, evaluates coth(ω/2T)·(G_R−G_A) over an ω-grid
and hard-rejects T ≤ 0 — the falsifier itself is inoperable without the flow
(machine-checkable in-repo presence). The R4 atomisation tightens this: the state-half
(b) is ABSORBED into rung2's KMS content; a KMS state is invariant under a flow that must
exist on the background for the definition to refer to anything.

**Presence, rung1 (STRONG, as-banked).** The banked S_IF is written in single-frequency
form (`S_IF.md` §3: S_IF = ∫(dω/2π)d³k [h_a·K_R(ω,k²)·h_r + (i/2)h_a·N(ω,k²)·h_a]); the
ω-analyticity causality condition is homed at rung1 by the cluster's spine artifact; and
S1's own inventory presupposes the flow without thermality (causality gives retarded
support; collapse to one ω additionally requires t−t′ dependence). NOT ruled FORCED: the
strongest counter stands recorded — Feynman–Vernon/SK exists on general backgrounds, and
rung1's booked sources include CTP-on-FRW. Presence is ruled **as-banked**, and the flow
node's own overturning route stays live: exhibit a formulation with no K_R(ω,k) anywhere
and presence at rung1 falls.

**Representation, both (CORRECT).** R5's mandate names the field and the relation: "the
nodes that PRESUPPOSE it MUST GAIN depends_on EDGES to it," with presupposition defined
by conjunct 1 as discharge-blocking (established, stated, AND used). `depends_on` carries
one booked semantics — resident.py's docstring calls the graph "the DAG of rests-on
edges" and the same field feeds the ORPHANED-RESULT and lineage checks; it cannot mean
"rests on" for two checks and "annotation" for a third. The typed alternative
(`edge_note`) is licensed by the register's own precedent (`d5e9a99`: "rung3 was never an
actual dependency → edge-note") on factual NON-USE — a license unavailable where presence
holds. And the tier the rule forces is CHARTER §1's own vocabulary: derived-pending =
"derived modulo a named open input"; the flow is a named open input with a named
discharge route that retires it at zero and mechanically reverses the demotions.

**Strongest surviving counters (recorded, not adopted):** (i) booking-format dependence —
rung1's four prose-priced inputs have the same logical status and cap nothing; tier
consequence turns on booking format. This is P1B's exact open subject, now ACUTELY LIVE
in the confirming direction, and is not decided here. (ii) The conditional-theorem
reading of rung2 — a literature theorem shown AS a conditional, its antecedent's tier not
a pending input to its own proof. Rejected for the banked node as used (the gate runs
against the declared background), recorded as the sharpest form of the objection.

## 2. Referee dispositions of note

Twelve advocate points rejected by name (full record in `.json` and the workflow
journal), including: the resident rule-(3) comment as a rung1 exemption (scoped to
ORPHANED-RESULT, pre-omission); "derived-pending is a category error" (relies on §4's
sufficient condition, not §1's definition); the F2 disposition-menu argument (out of
scope on the protocol's own fence — and incomplete: `9c14dfa`'s own framing contemplated
tier movement as the reserved resolution route); the AMBIGUOUS advocate's
meaning-variance argument (conditional firing of a rule is not meaning-variance of the
edge); and — against the winning side — EDGE_CORRECT's behavioral-acquiescence point
(discounted: the record equally shows the question thrice reserved) and its over-read of
F2 as conceding presence.

## 3. Evidence-verifier corrections (the protocol inventory was SOUND_WITH_CORRECTIONS)

Five MINOR corrections to the builder's frozen inventory, all recorded: (1) rung2's own
`edge_note` is a Ruling-B split classification, not a non-dependency annotation — the
edge-typing precedent lives on `kr_contract_retarded_tier4` (`d5e9a99`), where it stands;
(2) "tier strings only" is true at the field level; the textual diff is a whole-file
reformat, so the parsed-JSON comparison is the authority; (3) "sole prose rationale is
the frozen RAI artifact's line" was overstated — the RAI artifact entered at `89445a8`,
AFTER the demotion commit, so within `67059a8` itself there is ZERO recorded rationale
(a stronger fact than the inventory's); (4) "Awaiting a computation ⇒ derived-pending"
lives in CHARTER §4 (Premature graduation), not §1 — §1's parenthetical is the
definition; (5) the item-9 stationarity parentheticals are the builder's physics
judgments, not sources.json content. Omitted evidence the roles surfaced and the referee
used: `9c14dfa`'s full message (the resident fired verbatim at wiring time; two tests
left red deliberately; "resolving it means moving rung1's tier, which the owner reserved
for its own pass"); the Ruling-B edge lineage (the edge survived a deliberate owner
edge-reattachment audit, `04dc7e1`/`1459a2d`); `rung1_ontology_finite_memory` carrying
the identical edge at zero tier consequence (assumed-on-assumed is consistent — rule
firing is conditional, not meaning-variant).

## 4. Fixed-point determination (branch's own recorded run, 2026-09-13)

1. **Global no-under-propagation scan** (resident constants verbatim: RESULT_TIERS =
   {shown, derived}, OPEN_TIERS = {to-derive, assumed, derived-pending}): **ZERO**
   RESULT-tier claims with a direct `depends_on` into an OPEN-tier claim, over all 74.
2. **No-over-propagation** (field-level JSON diff `d5e9a99..67059a8`): tier-changed set
   is exactly the eight, `tier` field only, non-tier fields byte-equal. Cascade
   re-derived: rung1/rung2 forced by the adjudicated edges; kr_tier4 and rung4 via
   rung1+rung2; passivity via rung2; u1 via rung1; rung7_w1 via rung1+rung4; info_i1 via
   rung1+rung2. **Unforced members: NONE.**
3. **Landing tier**: derived-pending ∈ OPEN_TIERS; the rule fires on RESULT tiers only —
   no new flags (covered by scan 1).

**The propagated register is the fixed point of the booked tier rule. This ruling forces
zero tier edits and zero edge edits.**

## 5. Prose-repair queue (executed same date, append-only, none moving a tier or edge)

(a) `rung1_inin_formalism.stance_justification` — dated correction appended (the tier now
DOES carry the wired fifth input; original sentence kept; P1B subject untouched);
(b) `rung1_ontology_finite_memory.statement` — dated stale-tier note appended
("(shown, +4)" was true at the split); (c) `background_time_translation_flow.ledger_note`
— dated pointer mapping `rung1_inin_action` to its Ruling-B successors; (d)
`provenance/expected_red.py` — the four-name comment corrected to the eight with the
git-diff authority cited. P1A stanza CLOSED with the ruling appended (MOOT history
verbatim); P1B stanza given its dated cross-reference (question untouched).

## 6. Not decided here

P1B (both answers remain available); the demotion's paperwork (out of scope per the
frozen protocol; stays as disclosed on `dc110ba`, frozen); P2/P3/P4/P5/P6; the |N| = 0
classification content; rung3_single_pole's separate flagged tier question; any
prospective schema legislation (a tier-contradiction waiver instrument; a
stance-compatible landing tier); Born 2A/2B (unauthorized, unchanged). Presence at rung1
is ruled AS-BANKED — the flow node's two overturning routes stay live, and the
static-patch discharge mechanically reverses the demotions as a register change requiring
its own re-certification.

---

*Result recorded 2026-09-13. Protocol frozen at `bbe49f2` was not edited. The dc110ba
certificate stays frozen; the certification status update is
`RAI_CAPABILITY_TEST_01_CERTIFICATION_02.md`.*
