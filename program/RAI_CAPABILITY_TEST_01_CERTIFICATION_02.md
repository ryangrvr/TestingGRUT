# RAI_CAPABILITY_TEST_01_CERTIFICATION_02 — certification status update after the P1A ruling

**Status statement:**

> **|N| = 0 stands CERTIFIED WITHOUT the P1A condition, on the same corpus and the same
> instrument: the condition carried by the `dc110ba` certificate is DISCHARGED IN THE
> CONFIRMING DIRECTION by P1A_EDGE_ADJUDICATION_01 (EDGE_CORRECT, both nodes, both
> sub-questions). The eight-node demotion was legitimately reachable under the
> represented dependency graph.**

This file is a status update citing `dc110ba`, which stays **frozen and unamended as the
historical audited state** (owner directive). Its sentence "by demotion rather than by
adjudication" was true when written and stays on that certificate as frozen history; the
adjudication has now been run (protocol frozen at `bbe49f2`, result in
`P1A_EDGE_ADJUDICATION_01.{md,json}`), and the certified content is the register as
booked under a now-RULED reading — previously an unruled one.

**A bookkeeping adjudication confirms bookkeeping.** The discharge of the conditional is
NOT evidence about GRUT's physics; the no-GRUT-confirmatory-weight discipline applies
unchanged, and the truth-fence stays verbatim in force: this certification bounds
generative reach, not physics' content — correspondence to truth remains uncertified.

## Basis

- **The ruling:** EDGE_CORRECT under the frozen, owner-commissioned protocol — presence
  PRESENT (rung2 FORCED; rung1 STRONG as-banked, its overturn route live),
  representation CORRECT (R5 wires a discharge-blocking presupposition into the
  register's one rests-on field; edge_note's non-use license is unavailable where
  presence holds).
- **Fixed point (the branch's own recorded run, 2026-09-13, not the referee's
  pre-check):** zero RESULT-on-OPEN violations over all 74 claims; the demotion set is
  exactly the eight, tier-field-only, every member cascade-forced, none over-demoted.
  The ruling forces zero tier edits and zero edge edits.
- **Reversibility, restated:** the demotions remain mechanically reversible on the flow
  node's static-patch discharge — a REGISTER change that would require its own
  re-certification, not a defect in this one.

## Residuals carried forward (undropped; the ruling's costs on the face of the update)

1. **P1B-SHOWN-ON-LEDGER-INPUTS — now ACUTELY LIVE in the confirming direction**
   (replacing `dc110ba`'s adverse-direction phrasing): the register carries a RULED
   format asymmetry — wired inputs cap result tiers; prose-priced inputs do not —
   pending P1B, which remains OPEN and un-narrowed (both answers available: wire the
   four prose inputs and cap, or rule prose-pricing sufficient).
2. **P5-RESIDENT-SCOPE** — OPEN, `MAY_HAVE_NO_SYMPTOM: yes`; failure mode SAFE (blocks,
   does not wave through); live under this green and invisible to it.
3. **Synthetic ORPHANED-RESULT fixtures** — the two `test_resident` guards still
   exercise the unchanged guard on in-memory promotions; no live result-tier node with
   non-empty `depends_on` exists for the flag to fire on.
4. **The P6 10 → 9 builder call** — disclosed on `dc110ba`, still owner-ratifiable under
   open P6; the 9 declared stale-net prose cases stay red per owner directive.
5. **The demotion's paperwork — NOT blessed.** The eight demotions were executed inside
   `67059a8` ("R1/R2 complete + format pass") with zero recorded rationale in the commit
   itself, via a disposition absent from the F2 review's three-option menu. The ruling
   legitimizes the *reachability* of the demotions under the represented graph; it does
   not retroactively adjudicate that execution's procedure, which stays as disclosed.
6. **Prose-repair queue executed** (append-only, same date): rung1's partially stale
   `stance_justification` corrected by dated note; `rung1_ontology_finite_memory`'s
   stale "(shown, +4)" annotated; the `rung1_inin_action` name mapped to its Ruling-B
   successors in the flow node's ledger_note and both stanzas; `expected_red.py`'s
   four-name comment corrected to the eight.
7. **Frozen caveats 2 and 3** of the RAI artifact carry forward unchanged (|N| bounds
   generative reach, not physics' content; novelty from new inputs is input-provenance).
8. **Born Phase-2A hostile audit / Phase 2B — NOT authorized**, unchanged.

## Gate state at issue (2026-09-13)

Recorded after the branch's edits: see the suite run banked with this commit — the
failing set must equal the declared set (the three declared reds: P6 stale-nets ×9,
BlindSafe ×2) with `expected_red.py` exit 0. Suite color was an output of the branch,
never an input to the verdict.

## Not closed, not narrowed, not touched

P1B, P2, P3, P4, P5, P6 (all open); the frozen RAI pair (byte-identical); `dc110ba`
(frozen); the |N| = 0 classification content (untouched by the ruling);
rung3_single_pole's separate flagged tier question; Q3 (OPEN); ledger delta 0; no
dispatch sent.

---

*Issued 2026-09-13, citing dc110ba (frozen), bbe49f2 (protocol), and
P1A_EDGE_ADJUDICATION_01 (result). Owner-commissioned; owner may overturn the ruling,
which would void this update and revive the dc110ba conditional.*
