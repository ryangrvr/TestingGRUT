# GRUT — A Responsive Open-System Framework

### A research program with an audited-discipline method and a resident consistency-AI — physics as its one tested sector

<!-- REGISTER-SYNC: 53 nodes, net +16 -->
<!-- REGISTER-TOTAL: 74 = 53 grut + 21 vacuum-cluster; nets +16 grut, +0 cluster -->
*Vocabulary gloss for outside readers (added 2026-08-10 after an external reviewer reasonably misread it): **net +13** is the blind SUM of the register's underived-input ledger — the standing price of every assumption, unchanged for weeks and meant to stay unchanged — not a count of new entries; **CLEAN** means the register is unmodified since the last accepted change. Neither is a progress metric.*

*Living document — the canonical GRUT book. Compiled 2026-06-27, reframed 2026-06-28. Kept consistent, claim-by-claim, with the machine-checked register (`provenance/claims.json`, validator GREEN at net +13 GRUT; 49 GRUT-scope + 21 vacuum-cluster = 70 nodes). Of the **+13**, **+8 rides on four declared `laundering_ok` waivers** (`rung1_inin_action` +3, `rung5_gr_limit` +2, `rung6_qm_limit` +2, `p_tt_ansatz` +1), each carrying a written stance justification — `validate.py` prints the waived total on its own face. This is the program's working record and its eventual Zenodo deposit.*

---

## What this is — read first (page one, no inflation)

GRUT is **not a Theory of Everything**, and the program has **retired the "derive everything" frame**. The filename `GRUT_ToE.md` is legacy (kept for link stability); the honest frame is **the closure of the responsiveness hypothesis** — not "explain all of physics," but "take one ontological bet seriously and chart exactly how far it is forced to reach." The book is organized around a single anchor question:

> **Anchor question:** *Given responsiveness + locality + causality + open-system dynamics, what is **universal** — forced for any such system — and what requires **additional microscopic input**?*

This is the **closure/universality frame**: the value is not a derivation of everything but a sharp boundary between what the hypothesis *forces* and what it *imports*. Concretely:

> **GRUT is a Responsive Open-System Framework** — a research program whose original, candidate contribution is a **method**: an audited discipline that marks, ruthlessly, where every claim is *shown* versus *assumed*, **made into working machinery** — a *resident AI that enforces consistency*, not one that generates text. (The method's own novelty is **not yet screened** — it owes its own novelty audit before it is deposited as "the contribution.") Its **physics content** is the responsive-vacuum ontology, a **no-go ledger**, and the **arrow-of-time existence/direction decomposition**. **Physics is the one tested sector** among aspirational others (cosmology beyond linear order, complexity, life, consciousness), which remain **open research program, not established content.**

The forward agenda — the four universality questions this frame opens — is recorded in [`GRUT_II_Agenda.md`](GRUT_II_Agenda.md). The human-readable crystallization of what currently survives is [`GRUT_I_What_Survived.md`](GRUT_I_What_Survived.md).

This contribution stands **whether or not** the central physical hypothesis is ever vindicated. Two candidates for a genuine, novel, non-borrowed *physics* differentiator have now been posed and independently screened — the founding ζ-regularization hypothesis and an information/distinguishability principle — and **both dissolved** (Vol 4). GRUT's novel-physics-prediction column is, honestly, **empty**. What is *not* empty — and is the reason to keep building — is the method, the ontology, the no-gos, and the arrow result.

**How the discipline is enforced.** Every physics claim below carries a **tier** and a **falsifying computation**, drawn verbatim from the register:

| Tier | Meaning |
|---|---|
| **shown** | borrowed, established physics, verified against primary sources |
| **derived** | GRUT-internal, confirmed |
| **derived-pending** | derived modulo a named open input |
| **assumed** | a named, imported input (not a result) |
| **to-derive** | open work |

A claim may carry a **sub-status** (e.g. `no_go_export`, `settled-negative`). The **dependency ledger** sums a signed `ledger_delta` per rung — the net count of underived inputs. **The win condition was never zero inputs** (no candidate theory of everything derives its inputs from nothing) but a *short, marked* list. Current net: **+13**.

**This is a clean, ground-up rebuild.** Nothing here is carried from a prior GRUT lineage (v2/v3/v4); earlier results have no standing until re-derived inside this register's discipline and banked. Every claim, number, and tier is checked against `provenance/claims.json`, never against memory or an earlier draft.

**Scope of validity.** Except where noted, physics results hold *within the framework's assumptions* — linear response, a Gaussian (quadratic influence-action) bath, the Schwinger–Keldysh / Born reduction — stated as results *under those assumptions*, not universal no-go theorems.

---

# Vol 0 — The discipline, the auditor, the provenance (the genuine contribution)

*Lead with this: it is what GRUT has actually built, and it is transferable beyond GRUT.*

The deepest commitment in this program is not responsiveness. It is the **discipline about responsiveness**: tiering every claim; refusing to let a natural-looking choice pass as a derivation; and computing the thing that could embarrass the theory and reporting it straight. Crucially, the discipline is **not prose — it is enforced code**, and the code is the program's first real deliverable: the seed of a "scientific operating system" in which claims carry *provenance* rather than rhetoric.

- **`provenance/claims.json`** — the tiered register: every claim with its tier, sub-status, sources, falsifying computation, and `ledger_delta`. This file is the ground truth this book is checked against.
- **`provenance/auditor.py`** — the **generalized discipline engine**. `audit(claims, source_ids, valid_tiers)` applies, to *any* claim set, the same checks: every claim **tiered**, **sourced** (sources resolve in the register), **falsifiable** (carries an overturning computation), and **not laundered** (no net-positive `ledger_delta` sold as shown/derived/derived-pending without an explicit stance flag; `assumed`+positive only warns). It is not GRUT-specific.
- **`provenance/validate.py`** — the GRUT gate: a thin wrapper running the engine on the register, plus one repo-specific invariant (the α-normalization single-count). **GREEN at net +13 (GRUT); 70 nodes total (49 GRUT-scope + 21 cluster).**
- **`provenance/bankgate.py`** — the **live bank-time gate** (added 2026-07-06): diffs the working register against an accepted baseline and runs the resident's `propose()` on every changed claim — **BLOCK** stops a bank, **FLAG-FOR-FIREWALL** surfaces it for adversarial screen + overseer verification before acceptance. This converted "self-auditing" from a library the tests exercised into a **property of every edit**.
- **`provenance/coverage.py`** — the **coverage map** (2026-07-06): every node carries `domain` + `grut_standing` (covered / borrowed / open field / silent, plus a named-absent gap list), so the register reads as a *map of known physics with GRUT's fills marked* — borrowed scaffold can never silently read as GRUT reach (machine-checked over-claim sweep in `test_coverage.py`).
- **`gate/kms.py`** — the FDT/KMS admission gate as an executable predicate.
- **`provenance/test_auditor.py`** — 16 stdlib tests mirroring the discipline; the full suite (auditor + resident + harness + bankgate + coverage + doc-sync) is ~99 tests, including regressions pinning every caught near-miss and a doc-sync check that the standing docs match the live register.

**The one calibration that matters: this machinery verifies *discipline*, not *truth*.** A wrong-but-well-provenanced claim passes GREEN; the net ledger is a *blind sum* and cannot catch a physics error or a double-count. The honest core of the eventual everything-machine is that its claims carry **provenance**, not asserted **correctness**. Over-claiming the auditor as "verifies physics" would be the exact failure mode the whole program guards against. *(Full note: `RESULTS_auditor.md`.)*

**Honest status of the machinery itself.** The auditor is a **working seed / proof-of-concept**, not finished, publishable platform software: a few hundred lines of pure-stdlib Python that enforce the tiering and anti-laundering rules on a real register. Its value is demonstrated (it has, this program, repeatedly blocked the author's own laundering attempts and caught stale or mis-attributed content), but the "resident AI that enforces consistency across a scientific corpus" is a **vision the seed points toward**, not a shipped product. We claim the seed and the demonstration, not the platform.

---

# Vol 1 — Foundations: the responsive-vacuum ontology and the constrained primitive

## 1.1 The premise

The generative vision behind GRUT is broad — **responsiveness** (the capacity to respond, retain, and scale) as the root of structure. The premise the theory can actually *stand on* is narrower and harder: **the gravitational vacuum is a responsive medium with finite memory, governed by a single in-in action, from which known physics is recovered as limits, and which names the inputs it does not derive.**

## 1.2 The mathematical primitive — a *constraint*, not an absorber

GRUT's primitive is the in-in (Schwinger–Keldysh) influence action **S_IF**, equivalently a response map **R : S → S** on the vacuum's slow sector. **It is presented here as a constrained object, and that is the whole point.** A primitive is scientific only to the degree that it **forbids**; a primitive that "derives everything" has derived nothing — that is the unfalsifiability engine the discipline exists to prevent.

**S_IF is shipped with its axioms** (the constraints that give it teeth):

- **Locality / finite memory** — the influence functional is local with a finite memory kernel (single-pole, the one novel structural commitment; §2.2).
- **Causality** — a background Lorentzian causal structure; the kernel is *retarded* (the existence half of the arrow; §2.5).
- **KMS / FDT** — in equilibrium the noise is locked to the dissipation by detailed balance, enforced as a *hard admission gate* (no kernel that fails it enters the foundation; rung2).
- **Gaussian / linear-response** — a quadratic influence action; the leading constitutive truncation.
- **Positivity** — a positive Källén–Lehmann spectral measure and a positive-semidefinite noise kernel.

**What R cannot do (its value is its no-gos).** Because the primitive is constrained, it *generates* prohibitions — and those prohibitions are GRUT's strongest, most defensible exports (the no-go ledger, §2.4). The primitive forbids: a μ=4/3 linear-cosmology modification (excluded → ΛCDM); an economical single-mode evolving w(z); an observable parameter-free decoherence wedge; an observable GW-dissipation signature; and it **cannot** select the Einstein–Hilbert action or derive the Born measure (both borrowed). A reader should hold the primitive to this standard throughout: *what does it forbid here?* If the answer is "nothing," the statement is ontology or interpretation, not physics. **S_IF is not a flexible history-map that bends to fit any phenomenon — it is fixed by its axioms and audited by what it rules out.**

## 1.3 The foundation — four legs, all borrowed, all verified

| Leg | What it provides | Primary source | Tier |
|---|---|---|---|
| In-in / closed-time-path effective action | A vacuum with memory and a causal arrow: retarded kernel K_R + noise kernel N | Schwinger 1961; Keldysh 1964; Feynman–Vernon 1963; Calzetta–Hu (CUP 2008) | shown |
| Mori–Zwanzig projection / generalized Langevin equation | "Slow variable + fast bath + memory kernel" | Mori 1965; Zwanzig (OUP 2001) | shown |
| Fluctuation–dissipation theorem + KMS condition | Locks noise to dissipation; the equilibrium constraint as a hard gate | Callen–Welton 1951; Kubo 1966 | shown |
| Trace-anomaly (α) leg | A non-arbitrary contribution to the vacuum response coefficient, anchored by the a-theorem | Riegert 1984; Komargodski–Schwimmer 2011; Antoniadis–Mottola | shown / split (§2.4) |

Every load-bearing attribution was verified against the actual papers. The legs are **borrowed**, correctly — the framework's foundation is established physics, not novel invention.

---

# Vol 2 — Physics: the one tested sector

*This is the only sector with banked, tiered content. Everything here is checked against the register; everything above physics (Vol 3) is open program, not content.*

## 2.1 The derivation ladder

| # | Rung | Tier | Ledger Δ | Differentiates? |
|---|---|---|---|---|
| 1 | In-in influence action S_IF (the stance) | shown | +3 | no (structural) |
| 2 | KMS/FDT locks N to Im χ; enforced as a hard gate | shown | −1 | no (by design) |
| 3 | single-pole / short memory | **derived-pending** (genuinely open; circular lean; §2.3) | 0 | deferred |
| 4 | Re χ = elastic (Love) response, KK-linked to dissipative Im χ | shown | 0 | no |
| 5 | GR limit (τ_c→0) via Jacobson Clausius relation | assumed | +2 | no — **GR borrowed** |
| 6 | QM limit: integrate out bath → Schrödinger + decoherence | assumed | +2 | rate only; Born inherited |
| 7 | Out of equilibrium: relaxing χ(ω) → evolving w(z) | to-derive | +3 | needs two modes; 3rd input = the 2026-08-02 single-departure-shape booking |
| 8 | Tabletop falsifier (energy-basis gravitational decoherence) | to-derive | +2 | **quiet-or-faint** |
| 9a | α *value* a/c=1/3 — conditional theorem, dimensionless axiom | shown | 0 | no (zero-credit axiom) |
| 9b | α *bridge* / c₀ normalization — adopted parameter, **settled-negative**, not forbidden | assumed | 0 | no (obstruction-backed) |
| — | The arrow of time | assumed | +1 | existence yes, direction no |
| — | Linear cosmology: μ=1 ΛCDM **no-go export** (`mu_linear`) | derived-pending | 0 | no (deflationary) |
| — | Pure-TT projector ansatz (`p_tt_ansatz`) | assumed | +1 | no (named input) |

**Net ledger: +13.** The KMS-locking of N still offsets an import; single-pole's and the α anchor's −1 credits are both suspended; linear cosmology adds the named pure-TT input; rung 7's third input (the single-departure-shape closure, booked 2026-08-02) is the program's first net move. GR and QM appear as `assumed` recoveries that *expand* the ledger — reported as exactly that: recoveries, not derivations.

## 2.2 The one *novel* result — genuinely open, with a circular lean: single-pole

**Status: genuinely open, `derived-pending`, ANCHOR-class (neither derived nor refuted).** Two specialists leaned opposite ways on whether single-pole survives; the disagreement *is* the result.

It was provisionally graduated to *derived* in two steps — a density-of-states argument (a massless relativistic vacuum has DOS ∼ ω², giving super-Ohmic J∼ω³, short memory) and a finite-temperature analyticity check (the coth's 1/ω is exactly cancelled by one power of ω in J, so S(ω)=aω²+bω⁴+… is analytic at ω=0; `calc/finite_T_exponent.py`). **Then the re-opening, weighed honestly:** the analyticity argument tacitly assumes a *collisional / analytic-noise* bath. A free gas has infinite shear viscosity → free-streaming → a branch cut dissolves the single pole; but on de Sitter that flat-space intuition does **not** cleanly transfer (Gibbons–Hawking is observer-dependent, the Bunch–Davies correlator is analytic by microcausality, super-horizon freezing suppresses the streaming). Both defensible → **genuinely open.**

**The circularity is tight:** what forces the analytic class is *locality / finite-memory of the influence functional — which is single-pole restated.* So the favorable lean carries almost no independent evidential weight; the genuinely-earned content is one small thing: within the assumed class, the finite-T factor could have manufactured a slow second pole and does not. The banked open object is a **two-level transport-self-energy frontier** (Σ in G_R = 1/(G₀⁻¹ − Σ); the decision is a one-bit low-ω scaling class, not "one number from one vertex"), **frontier-reserved** (Vol 4).

**The spine test was then RUN (2026-07-04), and the disposition externally checked.** Loaded onto the Tan–Tsamis–Woodard computed pure-graviton dS self-energy as an *extraction*, it returned **earned-under-determined** — the low-ω TT class lives in the gauge/scheme-dependent part of the object (the 30-year dS-IR controversy in local form). Three passes (in-house → referee → independent external specialist) each removed an over-claim and none moved the tier; the surviving form is deliberately **symmetric**: the perturbative TT sector provides no evidence for the branch-cut reading *and* none for the pole (a frozen mode function distinguishes neither), the un-resummed secular log establishes **no** cut (the ω→0 continuation of the *assembled* object has not been done), and the horn-conditional is **forward-only** — *naturally supported if de Sitter IR-screens* — not a biconditional (uniqueness unproven). At scaling level the fork sits at **O(G²)**, below current O(G) resolution (`calc/rung3_spectral_structure.py`). **A literature anchor now exists (2026-08-02, overseer-verified):** the open-EFT-of-gravity program (Salcedo–Colas–Dufner–Pajer, arXiv:2507.03103) parameterizes exactly this object *class* — a dissipative tensor friction Γ_T, explicitly distinct from the conservative α_M·H, with a mandatory stochastic source — so the dissipative graviton kernel + noise is a named mainstream object, carrying a first loose slot-bound |Γ_T| ≲ few×H₀ (a bound on the shared friction slot, **not** a decomposed measurement of GRUT's kernel, whose own induced Γ_T is **not yet computed** — `calc/gw_tensor_friction.py`, staged).

## 2.3 The α leg, split (rung 9)

- **rung 9a — the value, `shown`, zero credit.** a/c = 1/3 adopted as a **conditional-theorem dimensionless axiom**: *IF the conformal mode is the IR carrier THEN a/c = 1/3* (Komargodski–Schwimmer / Duff). The anomaly *coefficient* is cohomological / one-loop-exact, so the value anchors regardless of late-time dynamics; it carries **no anchor credit** (the −1 is suspended). The value is untouched, not refuted.
- **rung 9b — the bridge, `assumed`, settled-negative.** That a/c = 1/3 *normalizes* the TT response amplitude c₀ in K^R = α·χ·P^TT is **settled-negative** — obstruction-backed, **not forbidden**. c₀ = α is an **adopted phenomenological normalization** (full analysis: §2.4 entry 1).

*Discipline note recorded here: an earlier "α has no carrier / α is annihilated" framing over-stated it — α is a scalar prefactor; nothing annihilates a scalar. The tracefree P^TT has zero double-trace, so 1/3 is a hand-pinned structural label, not a contraction output. That is a channel/provenance mismatch, not an annihilation.*

## 2.4 The no-go ledger — what the primitive *forbids* (its strongest exports)

Each entry is stated at its **earned strength** (these are not interchangeable) with the **spec it imposes on any completion**. *(Standalone extract: `NO_GO_LEDGER.md`.)*

**Strength legend.** **FORBIDDEN** = structural impossibility — **this rebuild banks NONE** (reporting that absence honestly is part of the discipline). **SETTLED-NEGATIVE** = strong obstruction, not impossible in every extension (open to a named rescue). **EMPIRICALLY EXCLUDED** = killed by data. **INVISIBLE-BY-SUPPRESSION** = real but below threshold by orders of magnitude. **BORROWED** = imported, marked.

1. **The α-from-anomaly bridge — SETTLED-NEGATIVE** (`rung9b_bridge`). Three obstructions in order: PRIMARY projector orthogonality (trace = spin-0, TT = spin-2, g^μν P^TT = 0; 1/3 has no shown carrier in the TT channel); SECONDARY independent Ward identities; TERTIARY a UV-vs-IR no-RG-protection gap. The FDT/KMS cancellation is a *supporting* mechanism (it leaves c₀ free), not a fourth obstruction. **Spec:** a new operator identity (a metric-built scalar→TT intertwiner, or a CFT route where the c/C_T Weyl-sector coefficient normalizes the TT two-point), or accept c₀ as external input. The kinematic S⁴ Riegert/Paneitz **carrier**-identification is a *separate, open* front, not prejudiced by this bridge.
2. **The μ=4/3 linear-cosmology modification — EXCLUDED (structural + ~4σ-class joint empirical)** → the trace-only endpoint is dead; linear cosmology = ΛCDM under the *chosen* c₀=0, and within the computed lensing-bound window otherwise — x < ~0.59 central-inputs, loose-upper per F-MAP (`zeta_interior_family`) — (`mu_linear`, rests on `p_tt_ansatz`). Excluded by a separate-universe consistency no-go (EdS-quantified) **and** empirically: ISW-cross **computed ~2.0σ** (Σ-corrected; `calc/isw_exclusion.py` 2026-08-03 — the old ~32σ retired, impossible in that channel, capped ~9–12σ) + DESI Σ₀ ~3.5σ (independent; joint ~4σ-class). The TT-auto channel is a prospect (estimate-grade, owed), not a leg. A *no-go export* — empirically selected, not derived. **Spec:** *(marked 2026-08-03)* the derive-from-the-action route is closed at the symmetry level (CHOSEN); only the rung3 trace-correlator route remains (a new +1 — relocation, not discharge), or accept the ansatz.
3. **An economical (sub-CPL) evolving w(z) matching DESI — NOT EARNED** (`rung7_wz`, `rung7_w2_wa_sign`, `rung7_w3_nocrossing_export`; all `to-derive`). A single passive relaxor cannot produce DESI's w=−1 **crossing** (needs ≥2 modes; generic/Vikman, conditional on the open `rung3`); the conformalon cannot supply the second mode (no SY dynamical mass for the Δ₄ compensator; stress is w=+1/3; magnitude ~8× below DESI at N=60). **Sign retraction (2026-06-29):** the old "w_a>0 wrong sign" is *withdrawn* — the w_a **sign is frontier-indeterminate** (the second law fixes the dissipative branch on the phantom *side* w≤−1, not the w_a *slope*; the toy's w_a≤0 is a ζ=const artifact). The **sourced prediction is w=−1 flat**; any w_a≠0 (either sign) needs the `mu_linear`-excluded trace sector — *not* a DESI-predictor. The *structural* claim (a memory vacuum evolves w) survives but is weak. **Spec:** a genuine second, slow, sign-changing/active mode — paying the extra parameter (≈CPL parity).
4. **The tabletop decoherence falsifier — INVISIBLE-BY-SUPPRESSION** (`rung8_falsifier`). The diagonal coupling samples S(0)=0 (quiet); the off-diagonal wedge is 7–47 orders below detectability. **Spec:** a leading off-diagonal coupling sampling S(ΔE) at O(1), or a bath resonance.
5. **GW dissipation as a differentiator — INVISIBLE-BY-SUPPRESSION** (`rung4_love_kk`). Real (absent in lossless GR) but ~22–62 orders below threshold — structural Planck suppression. **Spec:** ω_c ∼ MeV–meV (grossly excluded) or a collective IR mode.
6. **Deriving GR from the in-in machinery — BORROWED** (`rung5_gr_limit`, +2). The Ward identity enforces only ∇_μT^μν and does not select the action. **Spec:** the microscopic input fixing G and the derivative expansion without importing the area law.
7. **Deriving the Born rule from decoherence — BORROWED** (`rung6_qm_limit`, +2). Decoherence selects a pointer basis; a preferred basis is not outcome selection. **Spec:** the outcome-probability measure.

These are not failures; they are the primitive doing the one thing a disciplined theory can — **telling you exactly what it cannot do, and therefore exactly what is left to build.**

## 2.5 The arrow of time — existence versus direction

When every empirical differentiator had fallen, one structural claim remained; we attacked it (`calc/arrow_origin.py`; standalone extract `ARROW_OF_TIME.md`; register `arrow_of_time`, **`assumed`, +1**). The decisive result is a clean decomposition:

- **Existence is intrinsic.** The influence functional supplies the existence and magnitude of dissipation — a retarded analytic self-energy, a positive Källén–Lehmann measure, a positive noise kernel — operator-identically, with no low-entropy *system* state required.
- **Direction is state-dependent (not intrinsic).** None of the above fixes the *sign*. For an equilibrium (KMS) bath the direction is set by low-entropy data on the **past boundary** in three interchangeable guises reducing to one initial-condition assumption (past-endpoint contour; passivity / KMS β>0, by Pusz–Woronowicz; factorization ρ_S⊗ρ_B). Outside KMS, by the chosen reservoir state in the Liouvillian. The runnable model makes this concrete: Γ(t) is even in t, the coherence fully recurs at t_rec ∝ N, the decay is fixed by the assumed state.

**The contribution is methodological:** it *locates* the arrow's single underived input — the passive, low-entropy past-boundary state — without laundering it. GRUT does not derive the arrow; it makes the Past Hypothesis the visible, labeled rung-2 gate every claim passes through. A real clarifying statement for the foundations-of-irreversibility problem (Boltzmann; Zeh; Albert; Price; Carroll); not a solution.

## 2.6 The four differentiators, reported straight

GRUT opened with the hope of a distinct observable signature; each was computed honestly; **none survived as a parameter-free observable wedge.** (a) Energy-basis decoherence falsifier — **quiet-or-faint** (diagonal coupling quiet, off-diagonal 7–47 orders below). (b) GW dissipation — **real but invisible** (~22–62 orders below). (c) GR recovery — **borrowed** (Ward identity doesn't select the action). (d) Evolving w(z) — **needs two modes** (the economical version not earned; the conformalon closure is threefold: no SY mass, w=+1/3 wrong EoS, ~8× below DESI at the grounded Q²≈5.53). Details and computations: §2.4 and `calc/RESULTS_*.md`.

**The corrected empirical account (2026-08-02; four-domain external hunt + overseer number-verification — `SIGNATURE_AUDIT.md`, dated section).** A pre-registered A/B/C-bar search across GW propagation, cosmology, lab/analogue, and transport, adversarially refereed and then number-verified against primary literature, banked: (i) the **scoped null** — *no observable above Grade C in the explored structure* (never "empirically silent" — a universal claim no search can establish); (ii) the **DESI anti-signature** as the live threat — the preferred w₀wₐ trajectory *crosses the phantom divide*, exactly the shape the passivity no-go forbids a single passive relaxor to produce (3.1σ honest headline, contested systematics; if it consolidates, GRUT-as-written dies in this channel); (iii) the **α_M ≠ Im Σ_R category fence** — standard-siren "friction" is Hermitian-action running-Planck-mass physics (redefinable, noiseless, sign-indefinite), so a detected Ξ₀ ≠ 1 could **never confirm** GRUT and bears on it only after a conservative-vs-dissipative decomposition; (iv) the **graviton-mass demotion** — the m ∼ H₀ dS-horn is untestable by dispersion *in principle* (Compton wavelength beyond the Hubble radius), so its consistency with bounds is vacuous. Net: GRUT's empirical content is **falsificatory only** — it can be killed (a genuine crossing, μ≠1, a decomposed dissipative detection) but not singled out; the confirming absences equally confirm ΛCDM.

## 2.7 The dependency ledger — the named inputs

Net **+13**. (The list names 19 dependency *items*; the net +13 is the signed `ledger_delta` sum — smaller because internal offsets, like the KMS-locking credit and the suspended single-pole and α-anchor credits, net against the imports, and several named items map to ledger-0 claims.) The full itemized list (19 named inputs) — stance/foundational (1–5), state/thermodynamic (6–7, incl. the Past Hypothesis), GR-limit imports (8–10), QM-limit imports (11–12), model commitments (13–15, incl. the split α normalization), out-of-scope Standard-Model spectrum (16), the collisionality of the bath / bath Hilbert space (17), the pure-TT projector ansatz (18), and the single-departure-shape closure of the out-of-equilibrium noise (19; booked 2026-08-02 — the collapse of the free non-KMS occupation profile to the (ε, τ₂) two-dial family). *Nothing in this list is laundered; each item is the precise object that would have to be supplied.* (Full list retained in `NO_GO_LEDGER.md` and the register.)

---

## 2.8 The two-moduli interior family (opened 2026-08-02) — the first parameterized empirical surface

The p_tt interrogation (§2.2 anchor; `p_tt_ansatz.boundary_condition`) established that the admissible quadratic-kernel family is **K = c₂·P^TT + c₀·P0s** — shear *and* bulk moduli — and that GRUT-as-written sits at the c₀=0 endpoint **by choice, not by symmetry**. This section is what lies between the banked endpoints (`zeta_interior_family`, `to-derive`/0; `calc/zeta_interior.py`, `calc/mu_slip_interior.py`).

**The family, exact in the inherited μ–Σ bookkeeping** (endpoints reproduce `calc/mu_linear.py` identically; x = the scalar-channel admixture, x=1 the trace-only endpoint):

| | |
|---|---|
| growth | μ(x) = 1 + xα (exact — the coupling enters the Poisson sector linearly) |
| slip | η(x) = 1/(1 + xα) |
| lensing | Σ(x) = 1 + xα/2 — the identity **Σ−1 = (μ−1)/2 holds for all x** |

**The window (anchor computed 2026-08-03, `calc/isw_exclusion.py`).** The old story — a ~32σ ISW anchor giving x < 1/16 with ISW binding alone — is **retired**: the computed ISW-cross exclusion at the endpoint is **~2.0σ** (1.97 central, Σ-corrected per the same-wave firewall; band ~0.6–4.8; a 32-class number is impossible in that channel, capped ~9–12σ — the model *suppresses* the signal to ~0.57× the template, and the banked mechanism direction was backwards), and at central inputs the cross channel alone no longer 2σ-excludes any x ≤ 1. The **binding inverts**: DESI Σ₀ lensing now binds the family window at **x < ~0.59** under the banked slip — *central-inputs and loose-upper per the named F-MAP fence* (DESI's Σ₀ is Ω_Λ(a)-shaped, the family's Σ−1 constant; the shape-weighted mapping is plausibly 2–3× tighter, edge ~0.2–0.35) — μ−1 ≤ ~0.20, Σ−1 ≤ ~0.10, slip deviation ≤ ~0.17 (loose-upper ceilings). The **low-ℓ TT auto-power** channel — a growing potential pumps low-ℓ TT regardless of sign — is where a 32-class (indeed larger) number could live: estimate-grade order-10²σ-class at x=1 with an estimate-grade 2σ edge **band x ~ 0.03–0.14** (filter/normalization-sensitive; low end at or below the retired 1/16) — but it is *not banked* (its rigorous calc is the **owed gate** for any interior-viability claim above x ~ 0.06). Non-emptiness of the window remains structurally guaranteed and is **not** the finding; the finding is the computed **cross number**, the **binding inversion**, and the **channel map**.

**Honest framing, both directions.** The family **allows** deviations up to the **~20%-level computed lensing edge** (percent-level again only if the estimate-grade TT-auto channel is confirmed by its owed calc) — it does **not** predict any (x has no observable floor). The computed-channel edge is honest but loose (the owed TT-auto calc likely re-tightens it substantially — the conservative direction for *allows*, the wrong one for tightness). Under the computed channels **x=α² survives everything**; x=α's un-disfavoring is **central-inputs and F-MAP-fragile** (the shape-weighted Σ₀ mapping plausibly re-disfavors it at ~2–3σ — plausible-grade, not computed; its cross leg is ~0.6σ post-correction). The owed auto calc is the natural-point discriminator. What stays open, named: the x↔c₀ action-level map (partly frontier-reserved), the one-dial model question (owned by the open `mu_linear` demotion screen), and the u5 dynamics classification. **Why this section exists:** until 2026-08-02, GRUT's observables either equaled ΛCDM exactly or sat far below measurement (7–47 orders in the energy-basis channel, ~10²²–10⁶² for GW dissipation, ~8× for the un-earned w(z) mode) — a contrast of *ceilings*; that story is now known to be conditional on a *chosen* c₀=0, and this is the first computed look past the choice, with the low-ℓ ISW as the live discriminator either way.

---

# Vol 3 — Above physics: the aspirational research program (OPEN, not content)

**Version II — the universality program — is now OPENED and STRUCTURED** (2026-06-29 → 07-04, all `to-derive`/ledger 0 under the `u0` charter: *every branch a constrained classification with explicit failure states, never an ontology to defend*). Its central question inverted to: *why do constitutive/response descriptions appear in every successful EFT?* Three frontiers (why-split `u3` → why-constitutive `u4` → which-kernel `rung3`), worked as a classification tree with two **classification-grade results banked at ledger 0** (results about the *classification*, not physics claims): the **u5 two-sector exclusion** (KMS excludes the driven-dissipative Keldysh sector; Lorentz + T_μν-conservation excludes the literal rest-frame Hohenberg–Halperin equations — the survivor is the relativistic-passive-KMS transport sector, count OPEN) and the **u6 order-parameter candidate** (the discrete {reversible-bracket + conserved-charge} pair — identified, *not established*; the sharpness proof is the same deformability computation u5's count needs, which reduces to "how many conserved charges beyond T_μν?"). See `GRUT_II_Agenda.md`. *(The sectors below remain fully sterile.)*

*This volume otherwise contains **no banked content.** Physics (Vol 2) is the one tested sector. Everything here is **open research program**, written as `to-derive` or un-posed, and is **not** asserted as established. Treating "responsiveness explains X" as content — for any X above physics — would undo the entire discipline. The load-bearing question for each sector is the same one that built the no-go ledger: **what does responsiveness NOT explain here?***

- **Cosmology beyond linear order.** Linear cosmology is settled (= ΛCDM, a no-go export; §2.4 entry 2). Nonlinear/structure-formation responsive-vacuum effects, and any beyond-ΛCDM signature, are **open** — un-posed pending a derivation that names its inputs. *Not claimed.*
- **Complexity / self-organization.** Whether "responsiveness" (response + retention + scaling) is a useful *organizing* lens for complex systems is a research hypothesis, not a result. No GRUT mechanism for emergent complexity is banked. *Open; not content.*
- **Life.** "Responsiveness explains life" is **explicitly not a claim.** Any bridge from the responsive-vacuum primitive to biology would require its own register, sources, and falsifiers, none of which exist. *Open; named here only to mark it un-posed.*
- **Consciousness.** Likewise **explicitly not a claim**, and the highest-risk sector for the explains-everything trap. The framework says *nothing* established about consciousness; asserting otherwise would be the exact unfalsifiability failure the discipline exists to prevent. *Open; not content.*

**Why these are kept sterile.** A primitive that "explains" life and consciousness by reframing has *forbidden nothing* and is therefore not doing physics (Vol 1, guard on the constrained primitive). The credibility of the whole program is precisely this refusal: the upper sectors stay open and empty of content until — *if ever* — a constrained, falsifiable, no-go-generating statement is derived and banked. Aspiration is allowed; **aspiration banked as content is not.**

---

# Vol 4 — The honest record: what is tested, what dissolved, what is open

## 4.1 The two novelty candidates — both posed, both dissolved

GRUT deflated every downstream physics branch (Vol 2.7); the two remaining candidates for a genuine, novel, *non-borrowed* differentiator were finally posed in the register and independently screened. **Both dissolved.**

- **The founding ζ-hypothesis** (`founding_h1/h2/h3`). The originating intuition — R has a two-state structure that is the physical content of ζ-regularization, with the double-slit as its setting — **survives as a research program, not a physical claim.** Its generic half is true but borrowed (H1, `shown`: a vacuum response admits two *levels of description*, not two ontological states; not uniquely GRUT). The crux (H2, `to-derive`, **frontier-reserved**) is reframed away from numerology: the *direct* 1/3 ↔ −1/12 bridge is refuted (factor-4 unexplained, wrong sign; α is a 4D heat-kernel ratio, not a 1D mode-sum), but the open question — *can R = √(1+α) be a spectral invariant R = f(Spec(L)) with ζ unavoidable and α emerging from Spec(L)?* — stays open for a specialist, numerology excluded. The double-slit anchor (H3, `to-derive`, **deferred**) needs a unique observable distinct from standard QM; the original mapping is analogy-only and settled-negative by rung-8.
- **The information/distinguishability principle** (`info_i1/i2/i3`). The candidate that GRUT's surviving machinery *is* information-theoretic — independently screened both directions and **dissolves into standard info-RG (already imported) + interpretation.** The strongest survivor (the Kubo–Mori/Bogoliubov quantum-Fisher metric on the KMS Gaussian vacuum) dissolves **by a theorem** — QFI = canonical-correlation = FDT for KMS Gaussian states (Petz) — so the whole information-geometry *is* rung2 relabeled, its payout the same 7–62-orders-below-detection magnitude already computed. Modular theory = KMS = rung2; QNEC/Bekenstein/Jacobson-equilibrium are universal, downstream of rung9a's parent or rung5. The decisive structural reason no info principle can rescue it: information results are **monotonicity** statements (a_UV ≥ a_IR), never an absolute *value* — they cannot derive α = 1/3 (heat-kernel/field-content). I1 is `shown`-generic (already in GRUT via rung1/rung2/rung9a); I2/I3 are screened-refuted/moot. **Backdoor-clean** — no re-smuggling of the refuted ζ.

**Net ledger unchanged at +12** through both tests — posing the candidates as open questions named no new physics input.

## 4.2 GRUT's novel-physics-prediction column is empty — and that is reported straight

After every branch and both root candidates: **GRUT has no banked novel physics prediction.** Its one *novel* structural result (single-pole) is genuinely open with a circular lean; its other internal derivations are borrowed-physics applications. The honest contribution is **method + ontology + no-gos + arrow** — not a new physical prediction.

## 4.3 What is open, and the decisive computations

Single-pole is genuinely open (§2.2); the conformal sector's late-time stability is contested (the two-point ⟨T_ab T_cd⟩ is uncomputed; ⟨σ²⟩ ≠ ⟨T_ab T_cd⟩); the α *bridge* is settled-negative (repair owed) while the α *value* is a safe conditional-theorem axiom; the two-scale IR mode (rung 7) and the off-diagonal energy coupling (rung 8) remain open. **All converge on one frontier object:** the exact small-ω scaling exponent of the spectral density ρ_σ(ω) for the vacuum's stress-coupling on de Sitter — Im⟨T_TT T_TT⟩(ω→0, k→0) for a free field in Bunch–Davies, free-streaming-vs-freezing the explicit competition. It is the most over-claim-prone number in the program, so it is **frontier-reserved: pinned in isolation by a specialist, never approximated in-house.** The one remaining (disfavored) spectral thread (`founding_h2`'s Spec(L) question) is independent and likewise frontier-reserved — disfavored because the natural spectral candidate, the anomaly heat-kernel that α *is*, already fails to yield ζ (§4.1). *(Briefs: `SPECIALIST_BRIEF_rung3_spine.md` — the governing dispatch; `SPECIALIST_BRIEF_1_bath_spectral_function.md` is **SUPERSEDED** by it (its scalar-proxy/inserted-coupling methodology fails the spine brief's kill-conditions); `SPECIALIST_BRIEF_2_alpha_carrier.md` Part 2's normalization question is stale (rung9b settled-negative) while Part 1 and the carrier question stay live.)*

**Frontier #3 — the L₀→0 redundancy, attempted and screened.** The one genuinely open derivation of the four foundational frontiers — *is the adiabatic dilatation an exact, unique redundancy of S_IF broken only by memory L₀, making memory the sole structure-creator?* — was posed (`l0_r1`/`l0_r2`/`l0_r3`) and attempted in-house (`calc/L0_redundancy.py`), default-BROKEN. Verdict (independent both-directions firewall): **rests on a presupposed input.** One piece genuinely holds and even *generalizes*: for any analytic causal memory kernel the dilatation-breaking density is B = ω²L₀f′(iωL₀) = **O(L₀)**, so the memoryless limit restores the redundancy *exactly* — but that coincides with the standard Weinberg adiabatic soft theorem (R1, generic), and it predicts nothing. The GRUT-specific crux fails: **L₀ is unique only *classically*** (the trace anomaly — `rung9a` — breaks dilatation invariance independently of memory, so "memory is the *only* structure-source" is too strong), and **the adiabatic mode is *presupposed*** — S_IF singles out dilatation of its own argument, but the identification of that with the *spatial* adiabatic dilatation is the separate-universe map imported from the assumed background diffeomorphism invariance. So R2 does **not** graduate `mu_linear` (its bridge is relocated, not derived; `mu_linear` stays a no-go export), and "memory = structure-creator" survives only as "the leading *classical-IR* structure-source." A foundational theorem at most — no new physics — and **frontier-reserved** for a specialist (the de Sitter S_IF variation + a from-the-action derivation of the ω↔x map), with the ceiling capped below the strong claim. The honest result — *the foundation rests on a named irreducible input* — is exactly the kind the discipline exists to produce.

**The current frontier set (2026-08-02, the parked queue).** Beyond the assembled ρ_TT extraction above: (1) **`calc/gw_tensor_friction.py`** (staged) — GRUT's induced cosmological Γ_T at ω∼H₀ from the admitted kernel, with the pre-registered test *does the local memory scale connect parameter-free, or does the bridge need a new inserted scale?* — the re-admit gate for the fenced hunt conclusions; (2) the **p_tt interrogation** (`BRIEF_p_tt_interrogation.md`, adversarially screened) — *is the pure-TT projector forced or chosen?* — the program's load-bearing question, since P^TT roots the scalar-sector silence and carries `mu_linear`, `rung7_w2`, and `rung9b`; (3) the **u5/u6 deformability RG** (fixed-point computation; reduces to the vacuum's conserved-charge count). Standing watch: **DESI DR3** — the scheduled arrival of the live threat's confirmation or dissolution.

## 4.4 What GRUT is (the honest summation)

GRUT is an **audited Responsive Open-System Framework**. It adopts the **right tool** (the in-in treatment of the vacuum as a finite-memory open system). Its **one novel physics result is small and largely circular** (single-pole). Its **genuine, durable contribution is methodological**: a discipline made into machinery — a resident consistency-AI, a tiered register, an audited dependency ledger, a no-go ledger, and the arrow existence/direction decomposition — that locates exactly where and in what form each claim is assumed. **This stands whether or not the central physical hypothesis is ever vindicated.** The premise that survived contact is the narrow one: *not that responsiveness explains everything, but that here is exactly where it does and where it does not — marked, not laundered.*

---

## Appendix — Provenance & reproducibility

Register `provenance/claims.json`, gated by `provenance/validate.py` (→ `provenance/auditor.py`) against `provenance/sources.json`, with `gate/kms.py` the FDT/KMS predicate and `provenance/test_auditor.py` the discipline tests. Calculations are pure-stdlib Python with `RESULTS_*.md` write-ups (`finite_T_exponent.py`, `energy_basis_decoherence.py`, `q1_energy_basis_magnitude.py`, `gw_dissipation_bounds.py`, `wz_dark_energy.py`, `arrow_origin.py`, `mu_linear.py`, the conformalon thread `two_scale_desitter.py`/`conformalon_joint.py`/`conformalon_q2_band.py`/`delta4_stability.py`). Standalone extracts: `ARROW_OF_TIME.md`, `NO_GO_LEDGER.md`, `RESULTS_auditor.md`. Governed by `CHARTER.md`.

```
python3 provenance/validate.py        # the gate: GREEN, net +13 GRUT (70 nodes total)
python3 provenance/test_auditor.py     # 16 discipline tests
```
Everything runs in seconds with no third-party dependencies — each result inspectable and reproducible as written.

---

## Changelog (living document)

- **2026-06-28** — **Reframed to the honest thesis: a Responsive Open-System Framework / research program, not a completed ToE.** Layered into Vol 0 (discipline/auditor — the genuine contribution, led with), Vol 1 (constrained primitive + ontology), Vol 2 (physics, the one tested sector — tiered exactly as banked), Vol 3 (above-physics sectors as OPEN program, not content), Vol 4 (honest record). Four discipline guards baked in: the primitive must forbid; the upper sectors stay open; framework-not-ToE on page one; the auditor is a seed, not a finished platform. No new claim, no ledger change (net **+12**).
- **2026-08-02 (rigorous pass)** — **§2.8 opened: the two-moduli interior family, exact in the inherited bookkeeping** (`calc/mu_slip_interior.py`; appended to `zeta_interior_family`; net +13, 45 nodes unchanged). The first pass's fences F1/F2 **close as identities**: μ(x)=1+xα exact, η(x)=1/(1+xα), Σ−1=(μ−1)/2 for all x (endpoints reproduce `mu_linear.py` exactly); F3 partly (x = pure amplitude rescale ⇒ N_σ=32x exact-in-bookkeeping; the anchor's O(1) softness remains, and is the anchor's). The 2σ edge confirmed on all three observables (μ−1 ≤ 0.021, Σ−1 ≤ 0.010, 1−η ≤ 0.020). Remaining open, named: the x↔c₀ action-level map, the one-dial model question (the `mu_linear` demotion screen's), the u5 dynamics classification. The family allows up to the edge; it predicts nothing (x has no floor). **The book gains its first new physics-volume section since the V1 close.**
- **2026-08-03** — **The load-bearing number computed — and the banked anchor retired** (`calc/isw_exclusion.py`; net +13, 45 nodes, no tier moves). The ~32σ ISW anchor gating `mu_linear` and the interior window was computed for the first time: cross-channel exclusion **N(1) ~ 2.0σ** (1.97 central, Σ-corrected by the same-wave firewall B1; band ~0.6–4.8) — a 32-class number is *impossible* in that channel (capped ~9–12σ for a signal-suppressing model), the banked mechanism direction was *backwards* (μ>1 suppresses potential decay; the model suppresses the signal to ~0.57×), and at central inputs the cross channel alone no longer 2σ-excludes the endpoint. **Binding inversion:** DESI Σ₀ lensing now binds the family window (x < ~0.59, central-inputs/loose-upper per the named F-MAP fence; was "ISW binds alone" at 1/16 — overturned). Natural points de-symmetrized: x=α² survives everything; x=α's rehabilitation is F-MAP-fragile. The endpoint exclusion survives **multi-leg** (separate-universe EdS-quantified + Σ₀ 3.5σ + cross ~2.0σ; ~4σ-class joint empirical + structural; headline re-graded from "EMPIRICALLY EXCLUDED"). The **low-ℓ TT auto channel** (estimate-grade order-10²σ-class at x=1, est. edge band x ~ 0.03–0.14) is the owed gate — likely re-tightening the true window to the retired-1/16 neighborhood. Demotion trigger: condition 1 fired, condition 2 did not (separate-universe leg scored usable-but-conditional, now EdS-quantified) — no tier move.
- **2026-08-02 (superseded 2026-08-03 on the window numbers — see the entry above)** — **The interior computed: the window edge and its binding structure** (`zeta_interior_family`, `to-derive`/0; net stays +13; 45 nodes). The first exploration past the c₀=0 choice, using **banked inputs only** (the `mu_linear` endpoints, the ~32σ ISW anchor, the verified μ₀/Σ₀ constraints): the leading-order family μ(x) = 1 + xα with linear ISW-significance scaling gives the **2σ window edge x < 1/16 — μ−1 allowed up to ~0.021** — with the ISW anchor binding alone (growth ~21× weaker; lensing 4–9× weaker under *any* slip, the (μ−1)/2 reading being exact in the inherited bookkeeping). Firewall-corrected framing: non-emptiness was structurally guaranteed (no finite bound can empty the window under linear scaling) and is **not** the finding — the computed content is the *edge* and the *binding structure*; the edge carries O(1) anchor-softness uncertainty both directions. The first region of the GRUT family whose observables are **allowed up to** percent level rather than 20+ orders suppressed: a parameterized empirical surface instead of a null by construction — the family *allows*, it does not predict. The GRUT-natural-point map is already discriminating: α-normalized couplings x=α and x=α² are both data-disfavored; only x ≲ α/16 survives. Fenced hard: toy/scaling interpolation (rigorous μ(c₀)/slip owed — the graduation path); does not demote `mu_linear` (feeds its open screen); does not rescue DESI (no-crossing untouched); banks no new input (the c₀=0 *choice* remains `p_tt_ansatz`'s +1). **The empirical story is inverted in kind: what was "invisible by construction" is now "constrained to a computed window by one measurement" — the low-ℓ ISW is the live discriminator either way.**
- **2026-08-02 (later)** — **The projector question ANSWERED (CHOSEN) + the program's first net-ledger move (+12 → +13).** (1) **The p_tt interrogation ran** (overseer-executed, five independent angles, each adversarially verified against five pre-registered kill-conditions; core computation replicated in exact rational arithmetic; converges with the in-house `eft_operator_basis` frame-level result from full Lorentz-covariant generality): **diffeo invariance buys transversality and stops one condition short of tracelessness** — Barnes–Rivers 6-dim → two Ward survivors P^(2) + P^(0,s); linearized EH is itself (1/2)k²[P^(2) − 2P^(0,s)], so GR's own kernel carries a scalar piece twice its spin-2 one and no principle can force TT-only; the Weyl route is blocked by rung9a's own anomaly import. In medium vocabulary: `p_tt_ansatz` ≡ (ζ_vacuum = 0), a **constitutive assertion**. The +1 does **not** retire; the one escape (a bath-microphysics trace-correlator vanishing) would cost a new +1 at rung3 — relocation, not discharge. Question re-homed to `u5` ("is ζ_vacuum = 0?"); `mu_linear`'s demotion **opened** as its own screen (its TT/trace-only dichotomy is two endpoints of a continuous family; ISW excludes the endpoint, not the interior); the {shear, bulk} interior stands as the unexplored, empirically-viable sector. (2) **The T-fork's sharpened follow-up reversed the builder's premature T_SUBSUMED banking**: the restriction of the out-of-equilibrium noise to the (ε, τ₂) two-dial family is itself an unbooked input (screen verdict RESTRICTION_UNBOOKED, overseer-verified on four double-count checks) — **booked as rung7's third input; net +12 → +13**, the first ledger move in program history, in the direction of *more* honesty about assumptions, not less.
- **2026-08-02** — **The verification wave: coverage map, observable hunt + overseer number-verification, the corrected empirical account** (net **+12**, **43 nodes** — 32 GRUT claims + 11 borrowed/open-field scaffold; tiers unchanged throughout). (1) **The register became a map**: `grut_standing`/`domain` on every node, borrowed-premise nodes (Born rule, entropy~area+Unruh, Past Hypothesis, Λ-undetermined), real-medium + stat-mech borrowed scaffold — all zero-credit; a **live bank-time gate** (`provenance/bankgate.py`) now intercepts every register edit, plus doc-sync tests. A live harness/resident closed-check divergence was found and fixed (regressions pinned). (2) **A four-domain observable hunt** (GW propagation, cosmology, lab/analogue, transport; pre-registered A/B/C bar, adversarial referee) followed by **overseer verification of every load-bearing number against primary literature**: banked the **scoped null** (*no observable above Grade C in the explored structure* — never “empirically silent”), the **DESI anti-signature** with four attribution fences (the live threat: 3.1σ honest headline, phantom-crossing shape the passivity no-go forbids), the **graviton-mass demotion** (consistency-is-vacuous; Trivedi–Loeb in-principle fence), the **α_M ≠ Im Σ_R category fence** (a detected Ξ₀≠1 could never confirm GRUT), and the **Γ_T literature anchor** (Salcedo–Colas–Dufner–Pajer, arXiv:2507.03103 — rung3's object class now has a mainstream name and a first loose slot-bound |Γ_T| ≲ few×H₀). **Fenced out**: the un-computed ≤ 10⁻²¹ effect figure (no calc exists — no falsifier, no banking; re-admit gate = `calc/gw_tensor_friction.py`, staged). (3) **The T/ledger fork adjudicated T_SUBSUMED** (equilibrium T = H/2π is Hadamard-forced and paid at rung5; the out-of-equilibrium departure is fully spanned by rung7's (ε, τ₂); a state-shape-dial fence added) — net stays +12. (4) **Statement/body integrity**: rung3, rung7, rung8, and the arrow's headline statements scoped to what their own bodies support; `l0_r3` disposition added; CHARTER §2 and `GRUT_I_What_Survived.md` over-grades calibrated to the ledger's own “obstruction-backed, not impossibility-proof” standard; `SPECIALIST_BRIEF_1` marked **SUPERSEDED** (its methodology fails the spine brief's kill-conditions); Brief 2 scoped partially stale. (5) **`BRIEF_p_tt_interrogation.md` drafted and adversarially screened** (forced-vs-chosen on the pure-TT projector — the program's load-bearing question — with pre-registered kill-conditions and a directional-optimism guard). **Parked queue**: `gw_tensor_friction.py` → the p_tt interrogation → external dispatches (rung3 spine brief); DESI DR3 is the scheduled live threat.
- **2026-07-04** — **The rung3 spine test was RUN, not assumed** (no graduation; `rung3` stays `derived-pending`, ledger 0, net **+12**, **32 claims**). Correcting a discipline lapse — "probably under-determined, defer" was a *prediction*, and the method forbids concluding from the literature instead of running the test. Loaded the probe onto the Tan–Tsamis–Woodard *computed* pure-graviton dS self-energy (arXiv:2103.08547) as an *extraction*, four grapples both-directions vs the firewall guards. Verdict: **earned UNDER-DETERMINED** — banking three grounded results the guess lacked: (a) a **TT-frozen lead** (the TT mode function is unchanged at one loop; the secular ln(a) growth is in the *spin-0* potentials, not the TT propagator G_R^TT probes → a real asymmetry favoring HOLDS, leading-order/simplest-gauge); (b) a **derived horn-conditional** — *GRUT's single-pole spine holds iff de Sitter IR-screens* (Tsamis–Woodard vs Higuchi/Marolf–Morrison); (c) an **earned ceiling** — the pure-graviton low-ω TT class is not extractable from the existing object, so the specialist dispatch (`SPECIALIST_BRIEF_rung3_spine.md`) is now **earned and precisely targeted** (the arXiv:2602.07908 assembled-observable frontier), not assumed. Banked into `rung3`'s boundary_condition; `STATE.md` updated.
- **2026-07-04** — **Version II structured: the u0 charter + the u4 classification tree** (no new physics; net **+12**, **32 claims**). Banked the **GRUT II charter** (`u0`, `CHARTER.md` §8 + `GRUT_II_Agenda.md`): *every V2 branch is a classification exercise with explicit success/failure conditions — never an ontology to defend.* Posed two classification branches of `u4`, both `to-derive` / ledger 0 / `depends_on []`: **`u5_constitutive_phases`** (classify the universality classes of χ(ω,k); first-class failure = only one stable class → responsiveness *unique*, still publishable) and **`u6_constitutive_order`** (does constitutive organization admit an order parameter with RG significance? monotone = corollary; first-class failure = reduces to existing RG monotones → info_i2-adjacent). Added the three-coupled-facets note to `u4` (assumption-space → theorem → interpretation; interpretation cannot precede the theorem). A **third** free-text-regex near-miss surfaced and was handled by correct data placement (a cross-reference to `info_i2`'s marker belongs in `tier_note`, not `sub_status`; `CHARTER.md` §7 corollary). See `GRUT_II_Agenda.md`.
- **2026-07-02** — **Version II sharpened: three frontiers, not two — Frontier 3 posed** (referee-adjudicated; no new physics; net **+12**, **30 claims**). Conceded catch: deriving coarse-graining does *not* hand you linear response — that follows only under extra conditions (weak coupling, Gaussianity, near-equilibrium, timescale separation). So three distinct layers, not two: **why-split** (`u3`, F2) → **why-constitutive** (`u4_constitutive_origin`, F3, new — *"given coarse-graining, why a response structure?"*, `to-derive`, **fenced both directions** against "emergent"/"forced") → **which-kernel** (`rung3`, F1). V2's central question **inverted** to the constitutive-universality one (*why do constitutive descriptions appear in every successful EFT?*); U1/U2 demote to GRUT-specific instances. V1 headline adopted verbatim: *"GRUT I … establishes the maximal closure of the responsiveness hypothesis under current physics…"*. `GRUT_I_What_Survived.md` gains a structural axis (survivors structural, deaths numerical; structural = robust-because-less-falsifiable = signature-null's other side). See `STATE.md`.
- **2026-06-29** — **Version II opened: the three universality entry claims posed, default-BROKEN** (no underived input added; net **+12**, **29 claims**). The universality question split into three distinct levels: **U1 form** (`u1_form_universality`, `shown`-generic/BORROWED — the S_IF = K_R+(i/2)N form is universal by Feynman–Vernon/Caldeira–Leggett, but that is standard open-system physics, *not* a GRUT result); **U2 kernel** (`u2_kernel_universality`, `to-derive` — the genuine frontier: is the specific response kernel UV-completion-independent across QG completions? rung3-coupled, likely under-determined out of the gate); **U3 origin** (`u3_split_origin`, `to-derive`, **FENCED** in `sub_status` — why is there a system/bath split at all? "emergent" is a hypothesis to test, never a banked conclusion). V2 is **structural, not empirical** (signature-null persists); modal outcome = structural-clarification + under-determination. See [`GRUT_II_Agenda.md`](GRUT_II_Agenda.md). (`GRUT_I_What_Survived.md` stays frozen as the 26-claim Version I deposit snapshot.)
- **2026-06-29** — **Version I close + universality reframe** (no new physics; net **+12**, **26 claims**). Retired "ToE / derive everything" → **the closure of the responsiveness hypothesis** (anchor: *given responsiveness + locality + causality + open-system dynamics, what is universal and what needs additional microscopic input?*). Froze two dead leads, **disposition-only** (no ledger change): the α→TT bridge (`rung9b`, settled-negative → **frozen**, reopen only on a new scalar→TT operator identity) and the **conformalon mechanism** (marked dead — the Δ₄ Paneitz field is too different to be the IR relaxation mode; the *separate* Δ₄-late-time-stability question stays open in `rung9a`). Reframed the bath: `rung1` (open-system ontology) is the **spine/bedrock**; `rung3` (the bath) is **universality-probe #1**, a layer above — `SPECIALIST_BRIEF_rung3_spine.md` marked **dispatch-ready, DEFERRED** (firewall's modal outcome under-determined is *anticipated*, not banked as a result). Crystallized the register in [`GRUT_I_What_Survived.md`](GRUT_I_What_Survived.md) (four surviving pillars: open-system ontology, responsiveness-as-constitutive, finite memory, self-auditing method — the last owes its own novelty screen). Opened [`GRUT_II_Agenda.md`](GRUT_II_Agenda.md): four universality questions, with Q1 (*is responsiveness emergent or fundamental?*) held **OPEN** — not pre-answered "emergent." Completed the **fourth-pillar gauntlet**: banked `method_novelty` (`to-derive`, ledger 0, +1 → **26 claims**) — the self-auditing method, screened on itself (overseer-adjudicated, *more* adversarial than the physics screens), is a weakly-novel **synthesis** of prior art (GRADE · Popper · Lakatos · W3C-PROV · blind-analysis · LLM chain-of-verification); **no novel component**; the "biggest contribution" framing **retracted**; not a banked contribution — owes independent external validation. The method, run on itself, honestly refused to overrate itself.
- **2026-06-29** — **rung7 w(z) — the sign of w_a attempted in-house + firewalled → no-crossing (held `to-derive`), sign frontier, sourced = w=−1 flat.** A single passive single-pole channel cannot cross w=−1 (generic/Vikman; conditional on the open `rung3` — a no-go cannot outrank its anchor). The old "w_a>0 wrong sign" reading is **retracted**: the w_a sign is frontier-indeterminate (the second law fixes the dissipative branch on the phantom *side* w≤−1, not the w_a *slope*; the toy's w_a≤0 is a ζ=const artifact). Sourced prediction = w=−1 **flat**; any w_a≠0 needs the `mu_linear`-excluded trace sector — *not* a DESI-predictor. Harness `forward_wz` softened in lockstep; its passive representative now carries a DESI-*sign* w_a yet stays laundering-flagged (the no-clean-match guarantee rests on the `evolving ⇒ needs_unsourced_input` invariant, not the sign). A resident floor-bug (negated-"refuted" false-close on the open `rung3`) was found and fixed en route. `rung7_w1_wz_map` shown-generic; `rung7_w2_wa_sign`/`rung7_w3_nocrossing_export` to-derive, ledger 0. Net still **+12**, now **25 claims**. (`calc/wz_sign.py`, `calc/RESULTS_wz_sign.md`.)
- **2026-06-29** — **Frontier #3 (the L₀→0 redundancy) attempted in-house + screened → rests on a presupposed input.** Exact-at-L₀→0 holds and generalizes to any analytic causal kernel (B = O(L₀)) but is generic (= the Weinberg soft theorem); L₀ unique only *classically* (the trace anomaly co-breaks); the adiabatic mode presupposed (the ω↔x map imported from assumed background diff-invariance); does **not** graduate `mu_linear`. Foundational-theorem scope, frontier-reserved. `l0_r1` shown-generic, `l0_r2`/`l0_r3` to-derive/screened-dissolves, ledger 0. Net still **+12**, now **22 claims**. (`calc/L0_redundancy.py`, `calc/RESULTS_L0_redundancy.md`.)
- **2026-06-28** — **Information-principle candidate screened → dissolved** (standard-info-RG-relabeled + interpretation; backdoor-clean; QFI=Kubo-Mori=FDT theorem the decisive step). `info_i1` shown-generic, `info_i2`/`info_i3` to-derive/refuted-moot, ledger 0. Net still **+12**, 19 claims.
- **2026-06-27** — **Founding root screened → survives as a research program, not a physical claim** (H1 shown-generic; H2 to-derive/frontier-reserved spectral question, direct numerology refuted; H3 to-derive/deferred). Net **+12**, 16 claims at the time.
- **2026-06-27** — `GRUT_ToE.md` compiled as the canonical theory book.

*Tiers, ledger deltas, and primary citations are drawn verbatim from the validated register. Where a claim is `assumed` or `to-derive`, it says so. That honesty is the deliverable.*
