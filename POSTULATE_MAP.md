# The GRUT Postulate Map

*A durable companion to `NO_GO_LEDGER.md`. The no-go ledger records what GRUT **forbids**; this map records what GRUT **assumes**, sorted by the kind of assumption — so no input can hide and no output can be mistaken for one. It is the honest answer to "what is GRUT actually betting, and where?"*

*The register's named dependency items net to the figure in the sync stamps below after the internal offsets (the KMS-locking credit −1, plus the suspended single-pole and α-anchor credits). This map sorts those items — and the program's results — into four bins. Kept consistent, claim-by-claim, with `provenance/claims.json`; the machine-checked counts and nets are the REGISTER-SYNC/REGISTER-TOTAL stamps below, never this prose (2026-08-17: the hand-typed header this line replaces had gone stale twice — the exact failure the stamps exist to prevent).*
<!-- REGISTER-SYNC: 53 nodes, net +16 -->
<!-- REGISTER-TOTAL: 74 = 53 grut + 21 vacuum-cluster; nets +16 grut, +0 cluster -->

---

## The four bins

### Bin 1 — Irreducible primitives (3): genuine posits, not even targetable for derivation

These are the bedrock. Each is an input GRUT *must* make; none has an in-house derivation path, and the discipline has positively established (not merely failed to find) that each is imported by every program of its kind. Banking them as **primitives** is honesty, not defeat — it says "here is exactly what we bet, and we do not pretend to derive it."

1. **The responsive-medium ontology + system/bath split** — `rung1_inin_action` (ledger items 1–2). The gravitational vacuum *is* an open quantum system described by a Schwinger–Keldysh influence action, with a system (slow/IR) ↔ bath (fast/UV) partition. This is the framework's ontological bet; a framework must bet *something*, and this is the something. **This — not the bath — is GRUT's spine / bedrock.** Irreducible by construction.
2. **The Past Hypothesis** — `arrow_of_time` (ledger items 6–7). The passive, low-entropy, β>0 past-boundary state that fixes the *direction* of time. Established by the arrow screen: the in-in machinery supplies the *existence* of dissipation intrinsically, but **no derivation escapes a low-entropy/typicality input** for the direction (Boltzmann onward; Bogoliubov/ETH just relocate it). Irreducible.
3. **The Born measure** — `rung6_qm_limit` (ledger items 11–12). Decoherence from the noise kernel selects a pointer *basis*; it does **not** supply the outcome *probability* (the improper-mixture objection). The Born measure is an inherited, irreducible postulate — proven-not-derived in-house.

> These three are the floor. They are **not** "open layers": there is no calculation that would discharge them, and the discipline has shown why for each. Anything claiming to derive one of them is laundering.

### Bin 2 — Open layers: assumed now, but with a defined path to derivation — the live frontier

These are the program's genuine frontiers: inputs today, *outputs in principle*. This is where new work changes the ledger — though for item 2 the surviving route to FORCED costs a new +1 at rung3 (relocation), so the NET would not move; the real ledger leverage is item 1's.

1. **The bath's pole structure / collisionality** — `rung3_single_pole` (ledger item 17). Single real pole (collisional, analytic, short memory) vs branch cut (free-streaming, collisionless)? Currently `derived-pending`. A **layer above the ontological bedrock**, not the spine itself. **Universality-probe #1** (`SPECIALIST_BRIEF_rung3_spine.md`, firewalled dispatch-ready, **DEFERRED**): does the forced graviton microscopic content flow to GRUT's constitutive response? Derive the transport self-energy Σ_R of the pure de Sitter graviton sector and read the pole structure off as an *output*, breaking the finite-memory≈single-pole circularity. Forced-not-chosen holds at the bath-content level (the pure-graviton dS self-energy is a defined, computed object); the relocation risk moved *downstream* to the extraction step, fenced by six mandatory guards + six kill-conditions. **Three first-class outcomes: HOLDS / REFUTED / UNDER-DETERMINED** — the last is the firewall's anticipated modal outcome (the effective question waits on the unresolved 30-year de Sitter IR controversy), **not a banked result**. The highest-leverage effective layer — `rung3 → rung7 → the no-crossing → the wₐ-sign` all funnel here.
2. **The pure-TT projector** — `p_tt_ansatz` / `mu_linear` (ledger item 18). The response's tensor structure K^R = α·χ·P^TT is *inserted*, not derived — and as of 2026-08-02 **known CHOSEN, not forced**: the p_tt interrogation closed the symmetry route (diffeo-Ward buys transversality, not tracelessness; two survivors P^(2)+P^(0,s); the 'derivable in principle from the action' hope is answered **NO** for the declared structure). Only a dynamical (bath-microphysics, rung3) route to FORCED survives, at the cost of a new +1. The interior the choice foreclosed is now the open parameterized surface (`zeta_interior_family`). The cosmology sector funnels here.

3. **The 4d-covariant gauge-orbit availability** — `rung1_inin_action`, fourth declared input (booked 2026-08-17 on the noise-accounting ruling; renamed at owner verification). The transversality-closure theorem consumes the Ward-sourced gauge-orbit zero at 4d covariant strength — exactly the completion `calc/RESULTS_operator_basis.md`'s KC5 record reserves ("P2 full-4d-invariant; P0s spatial-subgroup-only, Bardeen completion frontier-reserved"). **Explicit discharge condition:** when the KC5-reserved 4d/Bardeen completion lands, this +1 retires into that work's accounting. Until then it is an open layer with a named path, not bedrock.

> The open layers are independent (see *modular decomposition*). Resolving the bath does not resolve the projector, and neither builds the covariant completion — separate doors, not one.

### Bin 3 — Borrowings, with the loan recorded: things GRUT does not host

Not "open layers" — things GRUT does not host and therefore borrows, each at its own strength (2026-08-18: the previous heading and this line said "proven un-hostable"/"positively shown it cannot host", which over-grades the GR entry: its obstruction is an underdetermination plus recorded imports, not an impossibility theorem — the exact over-grading this program convicts its prior lineage of). The no-go ledger records these as exports; here they are recorded as the *imports* they force.

1. **GR / the Einstein–Hilbert action** — `rung5_gr_limit` (ledger items 8–10). The diffeomorphism Ward identity enforces only ∂_μT^μν; it does **not** fix the action (EH, R², f(R), Lovelock, nonlocal-curvature all satisfy the same identities). The SK formalism alone is not expected to generate the Einstein–Hilbert term; "everyone pays somewhere" (Jacobson imports area+Unruh, Sakharov UV matter). GR is **borrowed, not hosted** — GRUT's gravitational sector is, on current footing, a member of the emergent-gravity family, and that membership does not rest on a from-scratch GR derivation.
2. **The α→TT bridge** — `rung9b_bridge` (the suspended α-anchor credit, ledger item 13). The trace anomaly does **not** normalize the TT amplitude: **primary projector orthogonality** (trace = spin-0, TT = spin-2; g^μν P^TT = 0; no metric-built scalar→TT intertwiner). `settled-negative` — c₀ = α is an *adopted* DC normalization, obstruction-backed (repair owed via a new identity, **not** forbidden in every extension).

### Bin 4 — Derived-not-postulates: outputs and no-go exports — never inputs

Recorded here so a *result* is never mistaken for an *assumption*. These cost no ledger (or *remove* an input).

- **`rung2_kms_gate`** (`shown`, −1) — N locked to Im χ by FDT/KMS; *removes* an input.
- **`rung4_love_kk`** (`shown`) — Re χ KK-linked to Im χ; recovers worldline-EFT tidal structure.
- **`rung9a_value`** (`shown`, conditional theorem) — α = a/c = 1/3 from the anomaly coefficient (cohomological, one-loop-exact, state-independent), *given* the conformal-mode carrier.
- **`mu_linear`** (`derived-pending`, no-go export) — linear cosmology = ΛCDM; GRUT *forbids* the μ=4/3 modification its own coefficient naively suggests. Empirically selected, not predicted.
- **The no-crossing** (`rung7_w3`, to-derive, the robustly-supported piece) — a single passive channel cannot cross w=−1 (the second law fixes the *side*); rung3-gated.
- **The arrow's existence** (intrinsic, shown-grade) — dissipation's existence and magnitude are supplied by the in-in structure (vs the direction, which is Bin 1).
- **The dissolved-screen negatives** (derived no-results) — the founding ζ-hypothesis (→ research program, not a physical claim), the information principle (→ standard info-RG relabeled), Frontier-3 / the L₀→0 redundancy (→ rests on a presupposed mode), and the conformalon unification (→ cannot supply the IR mode). Each is a *derived* negative, not an assumption.

---

## One lens, two levers — resist the over-unification

GRUT is **one lens** (the responsive-medium / open-system / conformal vacuum) but it has **two levers that are distinct in kind and must not be unified:**

- **Lever 1 — the anomaly α** (`rung9a`): a **kinematic** quantity — cohomological, one-loop-exact, **state-independent** (a/c = 1/3). The *scale* lever. It enters as a number, not a dynamical field.
- **Lever 2 — the bath** (`rung3`, a layer *within* the `rung1` ontology): a **dynamical, state-dependent transport sector** (the fast modes' Σ_R). The *memory* lever — universality-probe #1, a layer above the bedrock, **not** the spine.

The temptation to make them one object — the conformalon doing double duty as both the anomaly carrier and the bath's IR relaxation mode (the rung7↔rung9 unification) — was **refuted**: no Starobinsky–Yokoyama dynamical mass for the Δ₄ Paneitz compensator; the anomaly-shift and cosmological-stress couplings are one nonlocal kernel (ratio O(1), not ≥7); the conformal-mode stress is w = +1/3 (wrong equation of state). **α is distinct-in-kind from the bath.** This is not bookkeeping — it is the load-bearing guard on universality-probe #1: promoting the conformal scalar from "anomaly coefficient" (Lever 1) to "dynamical bath field" (Lever 2) would be exactly the relocation the `rung3` firewall exists to catch. Two levers, two doors, kept apart on purpose.

---

## Modular decomposition

The register factors into independent modules — *independent precisely because the over-unification was refuted*. Each carries its own open question; resolving one does not move the others.

| Module | Claims | Open question | Status |
|---|---|---|---|
| **M1 — Transport / bath** | rung3, rung7 (within rung1) | the pole structure of Σ_R | **open layer; universality-probe #1 dispatch-ready, DEFERRED** — highest leverage |
| **M2 — Anomaly** | rung9a, rung9b | late-time stability of the conformal sector | value settled-given-carrier; bridge settled-negative |
| **M3 — Cosmology** | mu_linear, p_tt_ansatz, zeta_interior_family | ~~derive P^TT from the action~~ **answered CHOSEN (2026-08-02)**; live: the interior family + the `mu_linear` demotion screen + the ISW-anchor calc (owed) | the symmetry route closed; dynamical route = rung3's |
| **M4 — Limits** | rung5, rung6 | (none — borrowings) | theorem-forbidden borrowings |
| **M5 — Arrow** | arrow_of_time | (none — Past Hypothesis) | existence intrinsic / direction primitive |
| **M6 — Falsifier** | rung4, rung8 | a dedicated QNM/ringdown calc | signature-null (see `SIGNATURE_AUDIT.md`) |

**M1 is the priority** (among the *effective* layers — the ontological bedrock is `rung1`, Bin 1): the most claims funnel to the bath, and its open layer has a defined, firewalled test (Σ_R, deferred). M2/M3 are separable frontiers; M4/M5 are settled (as borrowings/primitives); M6 is signature-null. Universality-probe #1 is the highest-leverage effective move.

---

## The three foundational frontiers (the forward-uncertainty stack)

Beyond the open layers, the program's forward uncertainty localizes to **three distinct foundational frontiers** — a depth-stack, *not* a single question, and *distinct in kind from the two levers above* (which separate α from the bath *within* the effective theory; these three are the layers *below and around* it):

| stack level | frontier | question | sits |
|---|---|---|---|
| **why-split** | **F2** — origin of coarse-graining (`u3_split_origin`, fenced) | why is there a system/bath split at all? | *below* `rung1` (Bin 1 presupposes the split) |
| **why-constitutive** | **F3** — origin of the constitutive form (`u4_constitutive_origin`, fenced, deepest) | given coarse-graining, why a *response* structure rather than an arbitrary functional? | *below* U1 (which presupposes the constitutive conditions) |
| **which-kernel** | **F1** — the specific kernel (`rung3`, universality-probe #1) | what is GRUT's particular Σ_R? | Bin 2 open layer; gated on the dS-IR controversy |

**Do not collapse F2 and F3.** Deriving coarse-graining (F2) does *not* hand you linear response (F3) — that follows only under extra conditions (weak coupling, Gaussianity, near-equilibrium, timescale separation). The map earlier had two frontiers; the referee's conceded catch is that there are **three**. Finding the third is a **truer map, not a new move** — F3 is the *least* tractable of the three (foundations-of-EFT), so the forward-gating (external: `rung3` specialist / dS-IR / foundational-EFT progress) is unchanged.

---

## How to read this map

- A claim in **Bin 1** that someone says they "derived" → laundering. The discipline has shown these are imported.
- A claim in **Bin 2** that graduates → a real ledger change; relay-gated, firewalled (universality-probe #1 is exactly such a graduation attempt).
- A claim in **Bin 3** sold as "hosted by GRUT" → over-claim; they are borrowings by theorem.
- A result in **Bin 4** counted as an input (or vice versa) → a category error the gate exists to block.

*This is a standing reference. Update it when an open layer (Bin 2) graduates or a borrowing (Bin 3) finds its repair identity — and never let a primitive (Bin 1) quietly migrate to "derived."*
